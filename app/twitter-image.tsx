// Twitter card image — reuses the same branded 1200×630 OG card so the
// summary_large_image card matches the Open Graph preview exactly.
export { default, alt, size, contentType } from "./opengraph-image";
