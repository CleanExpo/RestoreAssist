{
  "name": "@restoreassist/pilot-tester",
  "version": "0.1.0",
  "private": true,
  "description": "Senior-PM canary harness — drives synthetic restoration jobs against the sandbox to catch quality regressions Playwright can't.",
  "type": "module",
  "scripts": {
    "run": "tsx src/index.ts run",
    "swarm": "tsx src/index.ts swarm",
    "dryrun": "tsx src/index.ts dryrun",
    "preflight": "tsx src/runner/preflight.ts",
    "validate:user-pool": "tsx src/runner/validate-user-pool.ts",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "tough-cookie": "^6.0.2",
    "undici": "^6.21.0"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
