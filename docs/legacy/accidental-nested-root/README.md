# Accidental nested root

Untracked scratch tree found at repo root (`Restore Assist/`). Contained only empty dirs + `next-env.d.ts` / `.DS_Store`. Archived here during root cleanup; not part of the product.
