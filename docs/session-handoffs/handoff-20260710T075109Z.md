# Session Handoff — 2026-07-10T07:51Z

**Scope:** RA-7026 Phase 2 "Margot for contractors" go-live + estate auto-merge fallout recovery.
**Repo:** RestoreAssist only.

---

## 1. Summary of what was done

- **Completed:** Cleared session cache (shredded the local re-engagement prod-token file, cleared scratch + 287 MB `.next`).
- **Completed:** Diagnosed and fixed **two red gates** blocking the contractor-assistant PRs, both caused by earlier PRs the **estate automation squash-merged to `main` while their CI was red**:
  1. **AI guardrail audit** — the new `/api/assistant/chat` route spent the platform OpenRouter key (RA-6921 P0 violation).
  2. **API route audit** — `/api/internal/send-reengagement` (from #1898, already on `main`) hard-errored `api-auth-required` (bearer-token route the audit didn't recognise).
- **Completed:** Rebuilt **PR #1901** as one clean commit off the new `main` carrying the **complete** remaining feature (inc2 work-context + inc3 UI) + both fixes. It is now **100% green and MERGEABLE**.
- **Completed:** Closed **#1900** as superseded (its content is inside #1901); recovered the stack after the squash-merge broke it.
- **Not done (founder-gated):** the merge of #1901 itself.

## 2. Where it started

- Request chain: "clear cache and prepare session-handoff" → mid-turn: **"#1900, #1901 merge issues - fix these until 100% green."**
- Branch at start: `claude/ra-7026-margot-contractors-inc3`. Constraints: agent PRs can't be self-merged; go-live of customer-facing AI needs founder review; merge-gate (opening/pushing ≈ merging).

## 3. Decisions locked + what shipped

**Decisions**
- The contractor assistant is a **client workload**, so it **must use BYOK** (`resolveWorkspaceAiKey(userId, "OPENROUTER")`), failing closed to **402** when the workspace has no key — never the platform key. (The platform-key + `:free`-model path stays reserved for admin-only personal Margot.)
- `app/api/internal/` is a **headless bearer-token service namespace** (like `cron`/`webhooks`) → added to the API-audit exempt prefixes.
- After the automation squash-merged inc1 (#1899), the stack was consolidated into **one PR (#1901)** rather than three; #1900 closed, #1899 already merged.

**Shipped (merged to main by automation, pre-fix):** #1899 inc1 endpoint (with the *old* platform-key route — now corrected by #1901). #1898 send-reengagement route.
**Green + MERGEABLE, awaiting founder merge:** #1901 (complete feature + both gate fixes).
Nothing else merged this segment.

## 4. Key files

| File | Status | Note |
|---|---|---|
| `app/api/assistant/chat/route.ts` | Modified | BYOK OpenRouter resolution, 402 fail-closed |
| `lib/ai/openrouter.ts` | Modified | added `createMargotModelWithKey()` (BYOK factory, keeps `:free` pin) |
| `app/api/assistant/chat/__tests__/route.test.ts` | Modified | BYOK mocks + 402-no-key test |
| `scripts/audit-api-routes.ts` | Modified | exempt `app/api/internal/` (bearer-token service routes) |
| `scripts/__tests__/audit-api-routes.test.ts` | Modified | regression test for internal-route exemption |
| `app/dashboard/assistant/{page,AssistantChat}.tsx`, `lib/assistant/grounding.ts` | Read-only carried | brought onto main-based branch unchanged from inc3 |

## 5. Running state

No local dev server or background process running. Branch `rebuild-inc3` (local) = `9af77282` = pushed tip of `claude/ra-7026-margot-contractors-inc3` (#1901).

## 6. Verification — exact commands

- CI (authoritative) on #1901 @ `9af77282`: **all gates SUCCESS** — Quality Checks, Route Safety Scan, Validate DESIGN.md, Agentic Browsing. `gh pr checks 1901`.
- Local (pre-push): `npx tsx scripts/audit-ai-call-sites.ts --gate` → exit 0, net-new 0; `npx tsx scripts/audit-api-routes.ts` → Errors: 0; `pnpm type-check` clean; `npx vitest run app/api/assistant lib/assistant lib/ai scripts/__tests__/audit-api-routes` → all pass; `pnpm check:no-emoji` OK.
- CI-parity note: DB-gated suites + full `next build` can't run locally (no Docker/DATABASE_URL) — verified via CI, where "Apply migrations", "Unit tests", and "Build" all passed.

## 7. Deferred + open questions

**Deferred**
- **Merge #1901** — Owner: founder. Blocking: unpoisoning `main` (until it merges, every new PR's Quality Gates is red on the two audits) + contractor-assistant go-live. Why deferred: agent can't self-merge + customer-facing AI go-live needs founder review.
- **Activation of the assistant** — `CONTRACTOR_ASSISTANT_ENABLED=true` is already set on Production (founder's "flip it"); on merge+deploy the surface goes live. Each contractor needs their **own OpenRouter key** in Workspace Settings → AI Providers or they get a 402.

**Open questions**
- Should the automation be paused? — Owner: founder. It squash-merged **#1898 and #1899 while their CI was red**, which is what poisoned `main`. Recurring pattern worth a standing fix.

## 8. Pick up here

**Start here:** confirm #1901 merged (or merge it), then verify `main` PR Quality Gates goes green for the next PR.
**Do not redo:** the BYOK fix, the api-audit exemption, the #1901 rebuild, closing #1900 — all done and green.
**First command to run:** `gh pr view 1901 --json state,mergedAt -q '.state + " " + (.mergedAt // "not-merged")'`

## 9. Risk notes

- `main` is currently **red on PR Quality Gates** for any new PR until #1901 merges (guardrail + api-audit). This is the poison from the two red auto-merges; #1901 is the antidote.
- Vercel preview checks on #1901 showed `?` (deploy status, non-gating) at handoff.
- Many stale local branches + 4 stashes present (pre-existing); not touched.

## 10. Handoff quality check

All claims above are backed by commands run this session. CI green is quoted from `gh` output, not assumed. Nothing claimed merged that wasn't. First command provided.

---
Handoff complete. Next safe action: founder merges #1901 to unpoison `main` and take the contractor assistant live.
