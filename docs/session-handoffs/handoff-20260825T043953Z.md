# Session Handoff — RestoreAssist launch copy, SEO export, and the CI unblock (PR #2031)

**When:** 2026-08-25T04:40Z · **Branch:** `claude/restoreassist-launch-copy-ehq127` · **PR:** [#2031](https://github.com/CleanExpo/RestoreAssist/pull/2031) (open, not merged)
**Phase 0 gate:** `scripts/handoff-loop.sh --quick` → **exit 1 (BLOCKED)**, log `.handoff-logs/handoff-20260825T043953Z.log`
**Gate failures are both pre-existing and in files this branch never touched** — see §6.

## 1. Summary of what was done

- **Landing positioning shipped.** Hero now leads with *"Restoration software that works for you. Not the insurer."* Two new sections render it: `#stance` (three commitments) and `#positioning` (the carrier-vs-contractor case). Copy lives in `HOME`, shared by all three landing concepts.
- **Two claims caught and corrected before they went out.** The owner's copy drafts contained (a) early-access/waitlist CTAs for a product that is live, and (b) "$99 flat, not per seat" — contradicted by a shipped per-seat SKU. Neither shipped.
- **"Australia's first" removed** from all five live surfaces — unsubstantiated market-primacy claim, ACCC exposure.
- **Repo-wide CI unblocked.** Ten workflows had been dying at `pnpm/action-setup@v6` since ~22 Aug. Quality Checks had not executed a test on any PR in three days.
- **Accessibility gap fixed** with a regression test (folio links announced only "01" to screen readers).
- **Two durable docs filed:** the launch copy pack and the Semrush SEO export (taken before the subscription lapses).

## 2. Where it started

- **Request:** the owner pasted a "Launch Copy Pack" (later superseded by v2) and an "SEO Export Pack", then asked to check a merge-failing PR, then pushed for completion at "AAA rating 100%".
- **Constraints honoured:** all work on `claude/restoreassist-launch-copy-ehq127` only — session rules forbid pushing to any other branch without explicit permission, which was requested twice and never granted.

## 3. Decisions locked + what shipped

**Decisions:**

- **Waitlist vs live trial → live trial.** Product sells a 15-day trial and $99/month plan today; a waitlist in front of a working signup understates it. Repo evidence: `funnel-launch-assets.test.tsx` asserts the trial CTA renders.
- **"Australia's first" → dropped.** Changed in `lib/brand.ts` too, not just the page — leaving it there would re-seed the claim into metadata via `BRAND.description`.
- **Pricing copy follows the code, not the intent.** See §7.
- **Baseline files not bumped, tests not skipped** to force green. Both remaining CI failures are reported, not suppressed.

**What shipped:** six commits, pushed. PR #2031 open, `mergeable_state: unstable` (no conflict; failing non-blocking checks).

| Commit | What |
| --- | --- |
| `61cdbfd` | Landing positioning — hero, `#stance`, `#positioning`, folio renumber, mobile hero padding |
| `bd2a443` | `docs/marketing/seo-export-2026-08-25.md` |
| `37589bc` | Review fixes — a11y `aria-label`/`aria-current` + regression test, MD040 fences, decision-status wording |
| `5759aa7` | `packageManager: pnpm@9.15.9` + `pnpm-lock.yaml` via `pnpm import` |
| `1d11369` | "Australia's first" removed from five live surfaces |
| `b41c8e0` | Real pricing model in copy; Google Play → coming soon; desktop-can't-capture stated |
| `<next>` | Copy v3 — verified pricing ($99 / 50 reports / $1.98), cost-transparency angle, this handoff |

## 4. Key files

| File | Status |
| --- | --- |
| `components/landing/home/homeContent.ts` | Modified — `HOME.hero`, new `HOME.stance`, new `HOME.positioning`, corrected pricing pillar |
| `components/landing/concepts/claim-folio/ClaimFolioLanding.tsx` | Modified — renders both new sections; `SECTIONS` gains `title` → `aria-label` |
| `app/__tests__/funnel-launch-assets.test.tsx` | Modified — new copy assertions + accessible-name regression test (31/31) |
| `lib/brand.ts` · `components/landing/home/FAQSection.tsx` · `app/contact/page.tsx` · `tools/remotion/compositions/industry-promo.tsx` | Modified — "Australia's first" → "Australian-designed" |
| `app/support/page.tsx` | Modified — Google Play "coming soon", desktop cannot capture on site |
| `package.json` · `pnpm-lock.yaml` | Modified / Created — the CI unblock |
| `docs/marketing/launch-copy-pack-2026-08-25.md` | Created — v2 channel copy, claims table, decision record |
| `docs/marketing/seo-export-2026-08-25.md` | Created — Semrush snapshot |
| `docs/marketing/index.md` | Modified — OKF index entries |

## 5. Running state

- No long-running processes. A `next dev` server was started for screenshot verification and killed.
- `node_modules` is now **pnpm-installed** (was npm). Re-run `corepack pnpm install --frozen-lockfile` if state looks odd.
- Three self check-ins were scheduled on PR #2031; the live one is `trig_01UmhhqvEHyHr7YzfKE61BVG`, firing 2026-08-25T06:10Z. **Cancel it if this work is abandoned** — `mcp__Claude_Code_Remote__delete_trigger`.
- PR activity subscription is active on #2031.

## 6. Verification — exact commands

```bash
export COREPACK_ENABLE_DOWNLOAD_PROMPT=0
corepack pnpm install --frozen-lockfile          # exit 0 under pnpm 9.15.9 (the declared version)
corepack pnpm exec vitest run --config config/vitest.config.js --silent=true funnel-launch-assets   # 31/31
corepack pnpm exec eslint -c config/eslint.config.mjs components/landing/home/homeContent.ts        # clean
npm run check:au-english                          # passes
corepack pnpm type-check 2>&1 | grep -cE "error TS"                                                  # 9 — none in this branch's files
```

**Gate BLOCKED — both failures pre-date this branch:**

- `type-check` — 9 errors in `lib/cloud-mirror/drive.ts`, `lib/google-drive.ts`, `lib/integrations/{ascora,xero}/upstream-errors.ts`. Root cause: two `google-auth-library` copies (10.5.0 and 10.9.1) that `googleapis` types won't unify across. Verified zero errors in any file this branch touches.
- `check:no-emoji` — `app/dashboard/addons/AddonsClient.tsx:38` (`⬡`, U+2B21). `git diff --name-only main..HEAD | grep AddonsClient` → **0 matches.**

**Visual verification done:** hero, `#stance`, `#positioning` screenshotted at 1440×900 and 390×844 against `next dev`. Mobile CTA/trust-strip overlap fix compared against a baseline capture of the same viewport on `main`.

## 7. Deferred + open questions

**Open questions — both need the owner, both block nothing I can do:**

1. **Field Technician Seat: flat unlock or per-seat?** The owner stated *"field service and other services are $11/month add-ons to open them."* Six of seven add-ons match that exactly. `lib/billing/technician-seats-addon.ts` does not — it is `perSeat: true`, billed $11 × a buyer-supplied quantity of 1–100. A five-technician business pays $99 + $55, not $99 + $11. Either the code or the intent must move; copy follows the code until then. **Note:** seat *enforcement* is deliberately deferred, so nothing currently blocks inviting technicians beyond purchased seats — the charge is effectively voluntary today.
2. **Permission for a separate branch** to land the two repo-wide CI fixes. Requested twice, not granted, so not done.

**Deferred work, with the fix already diagnosed:**

- **`type-check` / duplicate `google-auth-library`.** A `pnpm.overrides` pin to one version should resolve it. Belongs with `fix/main-tsc-upstream-error-casts`, already open on the neighbouring two casts.
- **DESIGN.md lint** — net-new lucide-react imports, baseline 365 vs current 370. The repo's documented convention is to bump `.github/design-md-lint.baseline.txt` with a written rationale naming the files (done 7 times). The 5 net-new files are not from this branch and can't be identified from a shallow clone. Also: the migration target the error names, `src/components/ui/marks.tsx`, **does not exist in this repo.**
- **`restoreassist.com.au` vs `.app`.** Eight files under `tools/remotion/` (incl. `RA_URL` in `lib/brand.ts` there) print `.com.au` as the CTA URL; the product runs on `.app`. `tools/remotion/VIDEO_AUDIT.md:212` records the opposite and is wrong. Verifying `.com.au` and 301'ing it to `.app` fixes all eight without touching a composition — and is action 5 in the SEO pack.
- **SEO actions** — RIA/IICRC ANZ listings, Zapier, catalogue submissions, tracing the Fiverr backlink purchase, disavow assessment. All manual, all in `docs/marketing/seo-export-2026-08-25.md`.

**Copy v3 landed at the end of this session** and supersedes v1 and v2. Every figure in it was verified against `lib/pricing.ts` (`amount: 99.0`, `reportLimit: 50`, `signupBonus: 10`, packs at 20/50/100) before being applied. One item could not be confirmed from the config and is flagged in the doc: whether the $99 is GST-inclusive — the add-on modules say so explicitly, the monthly plan block does not.

**A correction worth carrying forward:** early in the session I reported the DESIGN.md lint as failing on its banned-vocabulary scan. That was wrong — that scan is explicitly non-blocking in the script. The real failure is Phill Rule 1 (icon imports). The PR comment stating the wrong cause was superseded by a corrected PR body at 02:02Z.
