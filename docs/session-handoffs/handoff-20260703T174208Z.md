# Session Handoff — RestoreAssist SHIPIT launch-readiness

**Timestamp:** 2026-07-03T17:42:08Z · **Gate:** GREEN (clean, type-check, lint, no-emoji) · **Log:** `.handoff-logs/handoff-20260703T174208Z.log`

## 1. Summary of what was done
- **Completed:** Full SHIPIT healthcheck of RestoreAssist (7-lens parallel audit + machine oracle). **6 PRs shipped, merged, and prod-verified** (#1648 drying calculator, #1649 email pipeline, #1650 data integrity + CLAUDE.md restore, #1651 connections/status hardening, #1652 security batch, #1653 billing correctness). Built and proved a new global **`spec-development`** skill (billing cluster was its first run). Filed ~22 Linear issues + the Restoration Pulse epic (RA-6948). Set 3 prod env vars.
- **Partial / pending owner input:** email fully works only after Resend domain verification (RA-6955); connections endpoint 401s until its token is set (RA-6937); Google Drive standards retrieval needs the service-account secrets (RA-6934, folder ID already set).
- **Not touched (filed as backlog):** BYOK platform-key migration (RA-6932), trial/BYOK decision (RA-6933), credential encryption (RA-6935), integrations reliability (RA-6942), launch copy (RA-6944), disconnected surfaces (RA-6946), RestoreAssist-AI tier repair (RA-6947).

## 2. Where it started
- **Request:** "Full SHIPIT healthcheck… find all missing before going to live paying customers… update the Linear project." Then iterative: fix email → build spec-development skill → real-AU drying calculator → billing cluster via spec-development → merges.
- **Branch at start:** origin/main @ `dd907f39`. **Constraints:** PR-only, nothing merges/deploys without human; owner-gated for pricing/secrets/prod (launch-charter).

## 3. Decisions locked + what shipped
**Shipped (merged to origin/main, deployed to prod restoreassist.app, live-verified):**
- Billing collapsed to a single **$99/mo catalog**; STANDARD/PREMIUM/ENTERPRISE tiers retired; `/api/billing/checkout` deleted (live-verified 404); webhook-side one-time fulfillment; Stripe-list double-sub guard; grandfathered report limits (Yearly=70/Lifetime=999 preserved).
- Email: support-ticket reply loop, Resend `result.error` surfacing (SDK never throws — silent-failure class), 10s timeouts, prod URL fallbacks.
- Data integrity: de-fabricated S500 citation grounding, fake "S500:2025" editions corrected, citation gate extended, CLAUDE.md restored to valid UTF-8 + encoding CI guard.
- Security: enumeration guard, fail-closed rate limits (28 sites), paid-proxy subscription gates, dev/seed prod hard-stops, SSRF/webhook residuals.
- Drying calculator: real 230V AU equipment data + desiccant/AFU + power/cost model, GPP formula fix (~2.65x), corpus-verified §12.x citations.

**Decisions locked (founder-confirmed):** C1 collapse-to-$99 + retire tiers; C2 keep 50 reports/mo base; C3 retire Yearly $1188; C4 grandfather existing subscribers.

**Env vars SET in prod this session:** `RESEND_FROM_EMAIL`, `NEXT_PUBLIC_APP_URL`, `GOOGLE_DRIVE_STANDARDS_FOLDER_ID` (verified present).

## 4. Key files
| File / artifact | Status |
|---|---|
| `.harness/audits/{audit,review,enhance}-2026-07-03.md` | **Created, LOCAL-ONLY (uncommitted, NOT on origin/main)** — the SHIPIT audit record; preserve |
| `docs/superpowers/specs/2026-07-03-billing-correctness/{spec,plan,tasks}.md` | Created + merged via #1653 (on origin/main) |
| `~/.claude/skills/spec-development/` (SKILL.md + 4 references) | Created (global skill, outside repo) |
| `~/.claude/projects/-Users-phillmcgurk/memory/spec-development-skill.md` | Created (memory) |
| lib/pricing.ts, lib/billing/fulfill-one-time.ts, lib/report-limits.ts, app/api/webhooks/stripe/route.ts, app/api/create-checkout-session/route.ts | Modified + merged (#1653) |
| lib/equipment-matrix.ts, lib/equipment-power.ts, lib/psychrometric-calculations.ts, lib/equipment-calculator.ts | Modified + merged (#1648) |
| CLAUDE.md | Restored + merged (#1650) — now valid UTF-8 |

## 5. Running state
- **No background processes running.** All build/poll subagents completed. All temporary worktrees removed (wt-integrity/connections/security/billing/ra-email pruned).
- Prod deployment **Ready** (verified via `vercel ls`); last live checks `/api/billing/checkout`→404, `/api/health`→200, `/pricing`→200.
- Local checkout is on merged branch `feat/drying-calculator-real-au-data`, **6 commits behind origin/main**. `.harness/audits/` untracked.

## 6. Verification — exact commands
```bash
# Gate the tree (was GREEN this session):
bash scripts/handoff-loop.sh --quick     # log: .handoff-logs/handoff-20260703T174208Z.log
# Prod live checks:
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://restoreassist.app/api/billing/checkout -d '{}'  # expect 404 (tier route deleted)
curl -s -o /dev/null -w "%{http_code}\n" https://restoreassist.app/api/health                              # expect 200
# Full suite (billing cluster passed 3214 tests at merge):
npx vitest run
```

## 7. Deferred + open questions
**Deferred (build backlog, all filed in Linear):**
- RA-6932 BYOK platform-key migration (~18 report routes still on platform key) — Owner: dev — Blocking: no — Why: large, ties to RA-6920 epic.
- RA-6935 encrypt Ascora/DR-NRPG plaintext creds — Owner: dev — Blocking: no.
- RA-6942 integrations reliability; RA-6944 launch copy; RA-6946 disconnected surfaces; RA-6947 RestoreAssist-AI tier down — Owner: dev — Blocking: no.
- RA-6948 Restoration Pulse initiative (judge 84) — Owner: founder to greenlight P0 pilot.

**Open questions (need human):**
- RA-6933 trial/BYOK wall: managed trial key vs pricing disclosure — Owner: FOUNDER — Blocking: yes for public self-serve launch — Why: business decision.
- RA-6955 Resend records — Owner: FOUNDER (Resend dashboard signup) — Blocking: yes, ALL outbound email — Why: domain not verified (no DKIM/SPF); inbound bounces (no MX). **Highest-priority owner action.**
- RA-6937 CONNECTIONS_STATUS_TOKEN — Owner: FOUNDER — Blocking: connections endpoint (401s all callers until set).
- RA-6934 Google Drive service-account secrets (5 vars: GOOGLE_PROJECT_ID/PRIVATE_KEY_ID/PRIVATE_KEY/CLIENT_EMAIL/CLIENT_ID) + share folder with client_email — Owner: FOUNDER — Blocking: standards retrieval silently degrades to "general knowledge" — folder ID already set.

## 8. Pick up here
**Start here:**
1. If continuing dev work, reset a clean checkout to origin/main (this checkout is 6 behind on a merged branch).
2. Preserve `.harness/audits/*.md` before any `git clean` — they're the uncommitted audit record.
3. Chase the 3 owner env actions (RA-6955 Resend is #1); after each, redeploy + live-verify.
4. Next build candidate: RA-6932 (BYOK platform-key migration) or RA-6933 once the founder decides.

**Do not redo:** the 6 merged PRs; the billing catalog collapse (live); the spec-development skill.

**First command to run:**
```bash
cd /Users/phillmcgurk/RestoreAssist && git fetch origin main && git log origin/main --oneline -3
```

## 9. Risk notes
- Local checkout is stale (6 behind origin/main, on a merged branch) — do NOT build from it without resetting to origin/main.
- `.harness/audits/*.md` are uncommitted and local-only — a `git clean -fdx` would destroy them.
- Prod verified green via live HTTP + `vercel ls` Ready, but the FULL vitest suite was NOT re-run on this exact stale local checkout this turn (only the quick gate: clean/type/lint/no-emoji). The 3214-test pass was at #1653 merge time on the billing branch.
- Email code is fixed but outbound email STILL does not reach customers until Resend domain verification (RA-6955) — do not report email as "working end to end."
- Google Drive folder ID set, but retrieval stays in silent-fallback until the 5 service-account secrets are set.

## 10. Handoff quality check
- Gate actually run: yes (quick, GREEN). Full build NOT run (quick gate only) — noted.
- No claim of shipped without merge evidence: all 6 PRs confirmed on origin/main.
- No process claimed running: confirmed none.
- Owner vs dev work separated in §7. First command provided in §8.

Handoff complete. Next safe action: `cd /Users/phillmcgurk/RestoreAssist && git fetch origin main && git log origin/main --oneline -3` to confirm the merged state, then chase RA-6955 (Resend domain verification) as the top blocking owner action.
