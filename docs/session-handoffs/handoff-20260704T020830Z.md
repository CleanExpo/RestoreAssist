# Session Handoff — RestoreAssist follow-up fix train + Exa MCP + 2nd-brain wiki ingest

**Timestamp:** 2026-07-04T02:08:30Z · **Gate:** GREEN, zero skips — full standard gate (clean, generated-files, type-check, lint, no-emoji, DB-independent build, route-safety scan) on origin/main `a37d9eda` with deps installed · **Log:** `/Users/phillmcgurk/RestoreAssist-fix/.handoff-logs/handoff-20260704T020710Z.log`

## 1. Summary of what was done

**Completed — RestoreAssist follow-up fix train (the prior handoff's continuation):**
- Reviewed-then-closed the prior session's last item: PR #1691 was merged before review; post-merge adversarial review PASSED, RA-6968 Done, two residuals ticketed (RA-6981/6982).
- Ran an 8-cluster ultracode fix train over the 10 follow-up tickets (RA-6970/73/74/75/76/77/78/79/81/82): **10 PRs merged (#1693–#1700, #1702), every one adversarially reviewed with mandatory mutation tests** (6 pre/at-merge PASS, #1699 post-merge PASS with 9/9 mutations killed, #1700 FAIL → fix-forward #1702 merged on a probe-verified PASS).
- All 10 tickets Done in Linear with verdict comments; 4 sharper follow-ups filed: RA-6983 (analytics write-side `metadata` bug), RA-6984 (QBO/MYOB payment resolution poller), RA-6985 (DR-NRPG replay tolerance), RA-6986 (audit:ai helper-indirection evasion).

**Completed — side jobs:**
- **Exa MCP**: hosted server (`https://mcp.exa.ai/mcp`) connected at user scope with the founder's API key as `x-api-key` header (signed into dashboard.exa.ai via Google OAuth, phill.mcgurk@gmail.com; key stored in 1Password Unite-Group-Infrastructure → "Exa API Key"). Tools register on next session start.
- **2nd-brain vault**: all 1,365 Sources files ingested into `Wiki/sources/` (7 signal syntheses + 8 zero-loss catalogs), both Sources inboxes cleared (raw recoverable at snapshot `d864597`), then vault cleanup (32 spec files → `2nd Brain/Specs/`, Untitled/Welcome junk + stale hermes backup deleted). Pushed to brain-1 (`17d218e`).
- **Session cleanup**: 3 abandoned `.claude/worktrees` removed (all clean), scratchpad cleared, 2nd-brain git gc (39M→16M), pnpm store pruned (4,001 packages), fresh pickup checkout left at `/Users/phillmcgurk/RestoreAssist-fix` (origin/main, deps installed).

**Not touched:** owner-gated backlog (RA-6933 trial/BYOK disclosure — FOUNDER decision; RA-6934/6937/6955 env/dashboard actions), the new follow-up tickets (RA-6983–6986), the sketch stream (RA-6841/6848 In Review — separate effort).

## 2. Where it started
- Resumed from `handoff-20260704T003437Z.md` via /resume-from-handoff (MINOR DRIFT: #1691 already merged unreviewed — handled per that handoff's own contingency). Then "GO" on the follow-up tickets; mid-flight requests: Exa MCP setup, Google sign-in, Sources→Wiki ingest, vault cleanup, this handoff.
- Base: origin/main `7a8b06a4` at session start; now `a37d9eda` (this session's 10 merges + the parallel sketch stream's #1692/#1701).
- Constraints: PR-only, adversarial review + mutation test before merge, file-disjoint parallel fixers, no schema changes, owner-gated items untouched.

## 3. Decisions locked + what shipped
**Shipped to origin/main (all merged + reviewed):** #1693 notifications mark-read (RA-6973·1) · #1694 estimates scopeId ownership (RA-6970) · #1695 verify-sub fail-closed + portal return_url (RA-6978) · #1696 Stripe refund scoping + Ascora secret decrypt (RA-6976/6977) · #1697 analytics truncation signal + dead metrics (RA-6975·2,3) · #1698 guarded-atomic refunds + charge-before-create (RA-6981/6982) · #1699 webhook reliability 4-item cluster (RA-6973·2 + RA-6974) · #1700 + #1702 audit:ai gate hardening, module-scope singleton seed included (RA-6979).
**Shipped elsewhere:** brain-1 `d864597..17d218e` (ingest + cleanup); `~/.claude.json` Exa MCP entry; 1Password Exa item.
**Linear Done this session:** RA-6968, 6970, 6973, 6974, 6975, 6976, 6977, 6978, 6979, 6981, 6982. **Filed:** RA-6983, 6984, 6985, 6986.
**Decisions locked:** merged-before-review PRs get post-merge reviews with findings as fix-forwards (applied twice, worked); RA-6975 item 1 (ServiceM8 grace queue) deliberately NOT built — design change, documented on the ticket.

## 4. Key files
| File / artifact | Status |
|---|---|
| origin/main `a37d9eda` | 10 fix PRs merged + reviewed; gate GREEN |
| `/Users/phillmcgurk/RestoreAssist-fix` | Created — clean origin/main checkout WITH deps; use this for next work |
| Local checkout `feat/drying-calculator-real-au-data` | Stale, ~46 behind, branch already merged — reset to origin/main before use (do not build here) |
| brain-1 `Wiki/sources/` (7 signals + 8 catalogs) | Created + pushed; Sources inboxes cleared |
| `2nd Brain/Specs/` (32 spec files) | Moved (git mv, wikilink-safe) |
| `~/.claude.json` exa entry + 1Password "Exa API Key" | Created |
| Memory: `brain1-sources-wiki-ingest.md`, `claude-code-setup-and-connectors.md` | Created / Updated |

## 5. Running state
- **No subagents, workflows, or fixers running** — both workflows (wf_fe2712f8, wf_de1a4223) and all standalone agents completed; all session worktrees removed except the deliberate `RestoreAssist-fix` pickup checkout.
- One **ScheduleWakeup** may still fire (~12:12 local) with a stale "check workflow + PR #1702" prompt — everything it checks is done; it should no-op.
- The nexus loop actively writes to the 2nd-brain vault (Decisions/*.jsonl, Outcomes/) — expect unstaged churn there at any time.

## 6. Verification — exact commands
```bash
# Gate (already green this session; re-run on pickup):
cd /Users/phillmcgurk/RestoreAssist-fix && bash scripts/handoff-loop.sh
# Merged-state spot checks:
gh pr list --repo CleanExpo/RestoreAssist --state merged --limit 10
gh api repos/CleanExpo/RestoreAssist/commits/a37d9eda/check-runs --jq '.check_runs[] | "\(.name): \(.conclusion)"'
# Vault ingest:
git -C /Users/phillmcgurk/2nd-brain log --oneline -4 && ls /Users/phillmcgurk/2nd-brain/Wiki/sources/
# Exa (next session): tools mcp__exa__* should be in the deferred list; claude mcp list shows "exa ... Connected"
```

## 7. Deferred + open questions
**Deferred (dev, non-blocking — Owner: agent):** RA-6983 analytics write-side `metadata` (may need schema decision), RA-6984 QBO/MYOB payment poller, RA-6985 DR-NRPG replay window (mechanical), RA-6986 audit:ai helper-indirection seed; 2nd-brain ingest-classifier retune (CARSI tag catches US collision-repair news — flagged in `Wiki/sources/signals-carsi-2026-07-04.md`); Gemini corpus re-upload for the new wiki pages (no key on this Mac — swarm machine syncs from brain-1).
**Open questions (Owner: FOUNDER — blocking their features only):** RA-6933 trial/BYOK disclosure (blocks public self-serve launch); RA-6955 Resend domain, RA-6937 CONNECTIONS_STATUS_TOKEN, RA-6934 Google Drive service-account creds (env/dashboard actions).

## 8. Pick up here
**Start here:** 1) Work from `/Users/phillmcgurk/RestoreAssist-fix` (fresh, deps in). 2) Next highest-value dev: RA-6985 (mechanical, sibling pattern) or RA-6983 (needs a call-site check first). 3) Keep the engine convention: PR-only, adversarial review + mutation test, merge on PASS.
**Do not redo:** the 10 merged PRs and their reviews; the Linear closures; the vault ingest/cleanup; Exa setup.
**First command to run:**
```bash
cd /Users/phillmcgurk/RestoreAssist-fix && git fetch origin && git log --oneline -5 origin/main
```

## 9. Risk notes
- Local main checkout (`/Users/phillmcgurk/RestoreAssist`) remains on the stale merged drying branch — handoffs live in its `docs/session-handoffs/` (untracked), so don't delete it; just don't build from it.
- `pnpm store prune` removed 4,001 unreferenced packages — installs in NEW worktrees may be slower until the store re-warms (RestoreAssist-fix keeps its node_modules).
- 2nd-brain: any rebase there can wedge mid-flight due to the concurrent nexus writer (escape: commit staged resolution, `git rebase --quit`, re-point main). A rebase REWRITES snapshot SHAs — recovery pointer is `d864597` (verified on remote).
- Exa MCP key is in `~/.claude.json` headers (plaintext local config) and 1Password; dashboard account is Google-OAuth only.
- CodeRabbit review threads on the merged PRs were not individually resolved (merges were founder-driven); if branch protection later enforces conversation resolution, expect that friction again.

## 10. Handoff quality check
- Gate run: YES — full standard gate, GREEN, zero skips, log cited above (strongest gate state of any session in this stream).
- Shipped claims all have merge/push evidence (PR numbers + SHAs cited); Linear states verified via API.
- No process claimed running; the one pending wakeup is disclosed with expected no-op behaviour.
- Deferred vs owner-gated separated in §7; first command in §8.

Handoff complete. Next safe action: next session runs the first command in §8 from `/Users/phillmcgurk/RestoreAssist-fix` and picks up RA-6985 or RA-6983.
