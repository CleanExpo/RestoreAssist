# Session Handoff - 2026-08-16T13:35:00Z

> Phase 0 gate: `bash scripts/handoff-loop.sh` in `D:\RestoreAssist` — **`RESULT: BLOCKED
> — failing gate(s): type-check`**. That failure is a proven non-execution, not a type
> error; see §5 and §9. Log: `.handoff-logs/handoff-20260816T132052Z.log`.
> **Three PRs are open, all CLEAN and MERGEABLE with 4/4 CI checks green.**
> Nothing is running that this handoff depends on.

---

## 1. Summary

**Phase 0.5 state: SHIPPED** — with two recorded exceptions, both stated below rather than
smoothed over.

All session work is committed, pushed, and open as pull requests whose CI is green. Nothing
is merged; merges are founder-only per NORTH-STAR.md and were never attempted.

| PR | Head | State | Purpose |
| --- | --- | --- | --- |
| [#2016](https://github.com/CleanExpo/RestoreAssist/pull/2016) | `7486e3a3` | CLEAN, 4/4 green | Make release-gate evidence freshness able to fail; withdraw the points it was hiding |
| [#2017](https://github.com/CleanExpo/RestoreAssist/pull/2017) | `53d8532a` | CLEAN, 4/4 green | Commit the 11:14Z handoff, add `FOUNDER-QUEUE.md`, correct a superseded blocker |
| [#2018](https://github.com/CleanExpo/RestoreAssist/pull/2018) | `cb254212` | CLEAN, 4/4 green | Drop dead `force=true` from `.npmrc` — unblocks every future push (RA-7229) |

**Definition-of-Done result: 5 of 5, with two exceptions recorded.**

1. **Done or deferred with owner** — yes. Queue items 5–7 (drain Linear, owner-evidence prep,
   worktree cleanup) are deferred in §7 with owners.
2. **Tests actually ran green** — yes, **in CI**. `Quality Checks` (which contains the Unit
   tests step) is `SUCCESS` on all three heads. The *local* gate SKIPPED tests by design —
   the DB suites truncate `users`/`workspaces`/`organizations`, so the gate refuses to key
   them off `DATABASE_URL`. Local test state is therefore **not green, it did not run**.
   Only CI scores.
3. **`git status` clean** — yes. **`git stash list` is NOT empty**: `stash@{0}: On
   fix/build-sh-fail-closed: S6c guard WIP - not for this release`. Pre-existing, from a
   different branch and a different session, containing nothing from this session's change.
   Recorded as an exception rather than claimed clean.
4. **Work is PR'd** — yes, three PRs, each with a valid release-gate receipt.
5. **Demonstrable outcome** — partially. Prod surfaces were demonstrated in a headed browser
   (§6). The authenticated demo path could not be, and that is now RA-7228.

**SHIP-DELTA — what can a paying human do today that they could not yesterday: nothing.**
Stated plainly, as NORTH-STAR.md requires. Nothing merged, so nothing reached production.
The delta becomes real on merge: #2018 unblocks every future agent push and speeds every CI
install; #2016 makes the gate's freshness rule capable of failing for the first time.

## 2. Where it started

A continuous compete-loop brief against `CleanExpo/RestoreAssist`, resumed from
`handoff-20260816T111435Z.md`. That handoff declared a hard stop: "there is no OpenRouter
key on this box", independent reviewer unavailable, nothing can push.

**That premise was wrong.** The key was already at `%LOCALAPPDATA%\hermes\.env`, and the
lookup chain that finds it was already recorded in memory — it had simply never been walked
before the halt was declared. No founder action was ever required.

Constraints carried: merges founder-only; the release gate blocks and must never be
bypassed; `or-review` mandatory before any push; blockers go to `docs/FOUNDER-QUEUE.md` and
Linear rather than stopping the loop.

## 3. Decisions locked + what shipped

**Decisions**

- **C2 is demoted to `fail`, not repaired.** Its scan could not have detected the live leak,
  so the pass was withdrawn rather than re-dated. It cannot return to `pass` until the
  credential is rotated — detection is not the blocker, exposure is.
- **The `ageDays < -1` future-date tolerance stays.** A reviewer asked to tighten it; declined
  with reasons. It is deliberate and test-pinned (`03d8f86a`): `verified:` parses at UTC
  midnight and Australian UTC+10 authors writing today's date sit briefly ahead of `now`.
- **No flake fix was built.** Characterisation refuted the premise (§7). Building one would
  have been work against a phantom.
- **Full YAML validation of frontmatter is out of scope** for #2016 — deferred to RA-7227
  rather than folded in, which would have made the branch unreviewable.
- **`owner-gated` used instead of creating a `founder-gate` label.** The existing label's
  description already covers secret rotation and merging to main; a near-duplicate taxonomy
  would fragment the board.

**Shipped (pushed, PR open, CI green — not merged)**

- `7486e3a3` — freshness ages from self-declared `verified:` date; `readUniqueKey` requires
  exactly one top-level frontmatter key; strict `---` fences; calendar-date round-trip;
  A3 demoted with a finding; C2 demoted to `fail`; 22 unit tests.
- `53d8532a` — handoff committed, `FOUNDER-QUEUE.md` created, supersession notice added.
- `cb254212` — `force=true` removed from `.npmrc`.

**Also changed, outside the repo** (`~/.claude`, not part of any PR):

- `settings.json` — fixed the `pr_release_gate.py` PreToolUse hook (unquoted path with
  spaces + a POSIX `/usr/bin/env python3`). Enforcement is live again, proven both ways.
- `scripts/or-review.ps1` — model set to `qwen/qwen3.8-max`, canonical secrets path added to
  the key lookup chain, `-MaxTokens` added, and an empty-review guard that now fails loudly.
- `secrets/openrouter.key` — installed from the hermes env file, after proving the path is
  gitignored.

## 4. Key files

| File | Status |
| --- | --- |
| `scripts/release-gate-score.ts` | Modified — `readUniqueKey`, strict fences, date round-trip |
| `scripts/__tests__/release-gate-score.test.ts` | Modified — 22 tests, 9 new |
| `docs/evidence/release-gate/1.0.0/C2-secrets-scan.md` | Modified — demoted to `fail` with the reasoning |
| `docs/FOUNDER-QUEUE.md` | Created — ranked founder-gated items |
| `docs/session-handoffs/handoff-20260816T111435Z.md` | Modified — supersession notice |
| `.npmrc` | Modified — `force=true` removed |
| `.gitleaks.toml` | Read-only inspected — the `(?i)\.md$` allowlist is the C2 root cause; **not** changed, needs RA-7224 first |
| `tsconfig.json` | Read-only inspected — excludes `scripts/**`; RA-7225 |
| `~/.claude/settings.json`, `~/.claude/scripts/or-review.ps1` | Modified — outside the repo |

## 5. Running state

Nothing is running. All background tasks from this session were completed or stopped.

**The Phase 0 gate verdict, read honestly.** The gate printed `RESULT: BLOCKED — failing
gate(s): type-check`. The wrapping shell reported exit 0, but that is `tail`'s exit code
from the pipe, not the gate's — do not read that as a pass.

`type-check` did **not** fail on a type error. It failed to start:

```
> NODE_OPTIONS="--max-old-space-size=8192" tsc --noEmit
'NODE_OPTIONS' is not recognized as an internal or external command
```

A POSIX env prefix that `cmd` rejects, so `tsc` never ran. PR **#2012** is exactly that fix
and is still unmerged. Run directly with the variable set the PowerShell way, `tsc --noEmit`
returns **rc=0 in 446s** on this branch.

Gate detail: `passed: clean generated-files lint no-emoji guards build (no-db) security-scan` ·
`skipped: tests` · `failed: type-check`. Note `guards` **passed** here, where an earlier WSL
run failed it — that earlier failure was a Windows-worktree gitdir artefact, not a content
problem.

## 6. Verification — exact commands

```bash
# Definition-of-done gate (from the repo root, native Windows git — not WSL)
bash scripts/handoff-loop.sh

# Type-check, the way that actually runs on Windows until #2012 lands (PowerShell)
$env:NODE_OPTIONS="--max-old-space-size=8192"; pnpm exec tsc --noEmit     # rc=0, 446s

# Scorer unit tests
pnpm exec vitest run --config config/vitest.config.js scripts/__tests__/release-gate-score.test.ts   # 22 passed

# Prod surfaces, headed browser (screenshots written to the scratchpad)
node demo-proof.mjs        # DEMO_BASE=https://restoreassist.app

# Independent review
~/.claude/scripts/or-review.ps1 -Path <file> [-Model openai/gpt-5.5] [-MaxTokens 16000]
```

Prod checked live: `GET https://restoreassist.app/api/health` → `{"status":"ok", …
"database":{"status":"ok","latencyMs":2}}`. Five public surfaces render 200; `/dashboard`,
`/dashboard/inspections`, `/dashboard/claims`, `/dashboard/field` and `/billing/upgrade` all
correctly auth-wall with a working `callbackUrl`.

## 7. Deferred + open questions

**Deferred**

| Item | Owner | Blocking | Why |
| --- | --- | --- | --- |
| Merge #2016 / #2017 / #2018 | Phill | Yes — all of it | Merges are founder-only; merge = production deploy |
| Rotate Stripe webhook secret, close alert #1 (RA-7224) | Phill | Yes — C2 cannot pass | Dashboard auth + credential decision |
| Demo credentials / seeded demo tenant (RA-7228) | Phill | Yes — demo proof, D1/D3 | Agents must not enter credentials |
| Merge #2012 (Windows type-check) | Phill | No — but see below | Verified working; makes the local gate honest on Windows |
| Branch protection: `allow_force_pushes` still `true` | Phill | No | One toggle |
| Trigger TestFlight build | Phill | Yes — E2 | Only external clock; Apple processing is not compressible |
| Type-check coverage for `scripts/**` (RA-7225) | Agent | No | Separate PR; will surface pre-existing errors |
| YAML-validate frontmatter (RA-7227) | Agent | No | Out of scope for #2016 |
| Queue items 5–7: drain Linear `sprint-0820`, owner-evidence prep (D1/D3/E1/E2/F1), worktree + stale-branch cleanup | Agent | No | Not started; ran out of session |

**#2012 — verified working, and it has only ever been waiting on a merge**

Raised during the session: "#2012 has been sitting without fixing for hours." It is not
unfixed — it has been `CLEAN`, `MERGEABLE`, 4/4 CI green since **2026-08-16T05:27Z**. The
only remaining step is a merge, which is on NORTH-STAR.md's immutable founder-only list, so
no agent could take it. What an agent *could* do — and had not — was prove the fix actually
works before asking for that merge. Now done:

| Check | Result |
| --- | --- |
| Runner resolves and spawns tsc on Windows | `node scripts/type-check.mjs --version` → `Version 6.0.3` |
| It type-checks covered files and reports real errors | planted `lib/__tc-canary.ts` → `error TS2322: Type 'string' is not assignable to type 'number'` |
| It exits non-zero on error | `EXIT=2` |

So the runner does the job the old `NODE_OPTIONS="…" tsc` line could never do on Windows,
**and it can go red** — which is the half that makes it a gate rather than decoration. Canary
removed; worktree confirmed clean afterwards.

It is `behind_by=5` from `main` but GitHub still reports `MERGEABLE`/`CLEAN`, so no rebase is
required. Safe to merge as-is.

**Open questions**

- **`sprint-0820` does not exist as a Linear label.** The brief assumed it. Create it, or name
  the real label to drain?
- **Which Qwen is "the latest flagship"?** `qwen/qwen3.8-max` (2026-08-03, 1M ctx) was wired
  as instructed, but `qwen/qwen3.8-2.4t-a95b` is newer (2026-08-12, 2.4T params). Moot for
  review use until the empty-content behaviour below is resolved.
- **Required approving reviews on `main` is `0`.** Set to 1?

## 8. Pick up here

**Do not redo**

- Do not hunt for the OpenRouter key — it is installed and positive-controlled.
- Do not re-fix the `pr_release_gate.py` hook — fixed and proven live both ways.
- Do not re-characterise the CI flake — done, premise refuted, RA-7226.
- Do not storm rival flake fixes — there is no genuine-flake cluster in the measured surface.
- Do not tighten the future-date tolerance — deliberate, test-pinned, declined with reasons.
- Do not re-review #2016/#2017/#2018 — each has a valid receipt bound to its exact head.

**Start here**

1. Confirm the three PRs are still CLEAN, then ask Phill to merge in order **#2018 → #2012 →
   #2016 → #2017**. #2018 first because it unblocks pushes; #2012 next because it makes the
   local gate honest.
2. After merge, dispatch `gh workflow run release-gate.yml --ref main -f strict=false` and
   report the score with a false-points split. Expect it to **drop** — C2's 5 points were
   withdrawn deliberately.
3. Then take queue item 5 (drain Linear) once the label question in §7 is answered.

**First command to run**

```bash
gh pr view 2018 --repo CleanExpo/RestoreAssist --json mergeable,mergeStateStatus,statusCheckRollup
```

## 9. Risk notes

- **`.npmrc` change has deploy blast radius.** Verified as far as is possible locally:
  `pnpm-lock.yaml` present, no `package-lock.json`, no `installCommand` in `vercel.json`, no
  DigitalOcean spec tracked, and the gate's own `provision()` completed in 16.4s. Vercel's
  real production build is only proven by the merge itself. Watch the first deploy.
- **A blank review reads exactly like a clean review.** `qwen3.8-max` returned *zero*
  characters of content while spending 16,000 completion tokens on reasoning. `or-review.ps1`
  now fails loudly on empty content, but any other harness calling that model has the same
  fail-open. Check `finish_reason` before believing silence.
- **`scripts/**` is not type-checked.** Proven by planting a type error and watching `tsc`
  exit 0 anyway. Any future "type-check green" claim about a `scripts/` change is unfounded
  until RA-7225 lands.
- **A pre-existing stash exists** (`stash@{0}`, from `fix/build-sh-fail-closed`). Not this
  session's, but it is uncommitted work someone intended to keep.
- **Six `wt-*` worktrees and ~29 stale remote branches remain untriaged** — queue item 7,
  not started. `D:\wt-audit` is still checked out at `7486e3a3`.
- **The `.gitleaks.toml` `(?i)\.md$` allowlist is still in place.** Narrowing it before the
  credential is rotated would produce a loud finding for a secret nobody has revoked yet;
  rotation first is deliberate sequencing, not an oversight.

## 10. Handoff quality check

- Tests claimed green: **CI only**, cited per-PR. Local tests SKIPPED and stated as not green.
- Shipped claims: each carries a PR URL and a head SHA; nothing claimed merged.
- Running processes: none claimed; all background tasks accounted for.
- The Phase 0 verdict is reported as **BLOCKED**, with the pipe-exit-code trap called out so
  the next reader does not mistake `exit 0` for a pass.
- Every "resolved" claim from the prior handoff that turned out false is corrected in §2.

---

Handoff complete. Next safe action: ask Phill to merge #2018 first — it unblocks every future
agent push and speeds every CI install, and nothing else in the queue moves until it lands.
