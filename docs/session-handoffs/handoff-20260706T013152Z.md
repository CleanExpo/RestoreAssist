# Session Handoff — RestoreAssist (2026-07-06)

**Scope:** RA-7000 Knowledge Wiki build + BYOK epic completion + Margot activation + data-integrity audit remediation (multi-agent `/nexus` session).
**Branch:** `main` @ `8082a704` (synced to origin/main).
**Gate:** green after dep sync — see §6.

---

## 1. Summary of what was done
- **RA-7000 Knowledge Wiki — Phase 1 (infrastructure) BUILT** as 3 non-overlapping PRs: **#1825 copyright-guard (MERGED)**, **#1826 AU/NZ localisation layer** (open), **#1827 foundation** = provenance/jurisdiction + retrieval split (open).
- **RA-6920 BYOK epic — COMPLETE + merged.** All 6 add-ons (FLOORPLAN/SERVICE_CRM/BOOKKEEPING/PAYMENTS/CLIENT_COMMS/VOICE) + per-seat billing scaffold (B6).
- **RA-6998 Margot — ACTIVATED + live** on a free OpenRouter model + workspace ElevenLabs Voice ID (off platform Anthropic).
- **RA-7001 data-integrity audit — DONE + live** (both RestoreAssist + CARSI; editions/§-citations/calc/units/spelling/regulatory), cross-validated vs iicrc.org. Units follow-up = **#1828** (open).
- **RA-7002 Costing Course** — research/design merged (CARSI#439).

## 2. Where it started
Continuation of a long `/nexus` swarm session driving RestoreAssist. This final stretch: build RA-7000 in parallel, activate Margot on OpenRouter + ElevenLabs Voice ID, and manage merges/CI. Constraints: agent opens DRAFT PRs, founder merges; zero-platform-cost/BYOK model; AU/NZ + copyright-safe standards.

## 3. Decisions locked + what shipped
Shipped (merged to origin/main, CI-green): #1801–#1818, #1822, #1824, #1825 (+ CARSI #435/#437/#439). Founder decisions locked: **S770 is fabricated → S700:2025**; **S760 = unpublished draft wildfire → removed**; **m² canonical area unit**; **mould remediation rate $65–145/m²**; **Margot: platform OpenRouter key + `MARGOT_MODEL=nvidia/nemotron-3-super-120b-a12b:free` (`:free` enforced)**. Real current editions: S500:2021, S520:2024, S540:2023, S590:2023, S700:2025.
Not yet shipped (open draft PRs): **#1826, #1827, #1828**.

## 4. Key files
| File | Status |
|---|---|
| `lib/rag/retrieve.ts` (+ prisma migration `20260706140000`) | Needs review — #1827 (provenance + retrieveForCitation/retrieveForReasoning) |
| `lib/anz/localisation.ts` | Needs review — #1826 (AU/NZ converters + AS/NZS maps) |
| `lib/standards/copyright-guard.ts` | Created + MERGED — #1825 |
| `app/api/margot/chat|hermes-proxy|voice/route.ts`, `lib/ai/openrouter.ts` | Modified + MERGED — #1824 |
| `lib/billing/*-addon.ts`, `addon-registry.ts`, prisma `FeatureEntitlement.seats` | Modified + MERGED — RA-6920 |
| `lib/stripe.ts` (apiVersion `2026-06-24.dahlia`) | Modified + MERGED — #1818 |
| `~/ra6934-populate-rag.sh` | Created (home dir) — founder RAG ingest script |

## 5. Running state
No background processes running. All session subagents completed. `caffeinate` (from an earlier overnight run) has long expired. `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, `GOOGLE_DRIVE_STANDARDS_FOLDER_ID`, `RESEND_FROM_EMAIL` set in Vercel prod; prod healthy (last checked db ~17ms).

## 6. Verification
- Gate: `bash scripts/handoff-loop.sh --quick` → initially **BLOCKED (type-check)** — proven a **stale-`node_modules` false-negative** (ff-pull brought new `@ai-sdk/openai` + Stripe SDK into package.json; `--quick` skips install).
- After `pnpm install --no-frozen-lockfile`: **`pnpm type-check` → 0 errors.** `clean`, `lint` (0 errors / warnings only), `no-emoji` all PASS.
- Tests + build: **not re-run locally**; authoritative evidence is CI — every merged PR passed full **Quality Checks** (type/lint/tests/build) on merge.
- Log: `.handoff-logs/handoff-20260706T013152Z.log`.
- Re-gate accurately next window with: `bash scripts/handoff-loop.sh --full` (installs deps first).

## 7. Deferred + open questions
**Deferred (Owner / Blocking / Why):**
- RA-7000 **Phase 2 consumer wiring** / me / no / held to avoid clobbering shared routes while 4 agents ran; do after #1826/#1827 merge.
- RA-6934 **RAG populate** / founder / YES for RA-7000 to go live / needs `GOOGLE_PRIVATE_KEY`+`GOOGLE_CLIENT_EMAIL` + IICRC/RIA PDFs; run `~/ra6934-populate-rag.sh`.
- CARSI 3 mould courses **SME publish-flip** (CARSI#437 merged as draft status) / founder / no / accuracy review before publish.
- Costing Course accountant verification + P&L-read calculator build / founder+me / no / RA-7002.

**Open questions (Owner / Blocking / Why):**
- Seat **enforcement**: define **Organization↔Workspace link** + base-plan included-seat allowance / founder / yes-for-enforcement / two disconnected tenancy models, no FK.
- **Negative-air / HEPA** rate values ($55–180 vs $85–185; $35–90 vs $45–95) / founder / no / true duplicates, founder never gave values.
- m² **mixed-unit** legacy data: spot-check real records (some historical rows may be m², some sq ft) / founder / no / migration assumed sq ft, reversible.

## 8. Pick up here
**Start here:** (1) confirm #1826/#1827/#1828 merged (verify green first); (2) build **RA-7000 Phase 2** — wire `retrieveForCitation`/`retrieveForReasoning` + `lib/anz/localisation` + `lib/standards/copyright-guard` into report-gen (`app/api/reports/generate-inspection-report` + `lib/standards-retrieval.ts` + knowledge-graph `expandContext`), the calc engine, and Margot; + a marketing zero-verbatim lint.
**Do not redo:** the audit remediation (RA-7001, all merged + live), BYOK add-ons, Margot activation, or the units sweep — all done.
**First command to run:** `cd ~/RestoreAssist && git pull --ff-only origin main && pnpm install && bash scripts/handoff-loop.sh --full`

## 9. Risk notes
- Local `main` was ~25 commits stale on entry (ff-pulled). Any fresh clone/worktree must `pnpm install` — package.json moved (Stripe SDK, `@ai-sdk/openai@3.0.80` pinned; **4.x breaks `ai@6` provider-spec v3** — do not bump).
- **Stripe `apiVersion` is now `2026-06-24.dahlia`** on prod — live payments surface changed (tests green).
- RA-7000 infra is built **ahead of the data** — the RAG is empty on prod until the RA-6934 ingest runs; nothing is grounded until then.
- Copyright guard (#1825) is a **heuristic, not a legal guarantee** (contiguous-verbatim only).
- Linear auto-closes issues when a merged PR title references their ID — re-open epics/trackers if wrongly closed.

## 10. Handoff quality check
Gate run + result recorded (§6, with the false-negative explained + evidence). Shipped vs deferred separated. No unverified "tests passed locally" claim (cited CI for tests/build). First command provided. Durable resume pointer also in memory `restoreassist-active-work-2026-07-06`.

Handoff complete. Next safe action: in a fresh window, run `git pull --ff-only origin main && pnpm install && bash scripts/handoff-loop.sh --full`, then merge #1826/#1827/#1828 and start RA-7000 Phase 2 wiring.
