# Session Handoff — 2026-07-29

Worktree: `/Users/phill-mac/RestoreAssist/.claude/worktrees/ra-sidekick-v1`
Machine: Mac Mini (`Phills-Mac-mini.local`) — the RestoreAssist box.

Supersedes `handoff-20260728T031500Z.md`.

## Summary

Continued unblocking main. `main` was red on both PR gates and, because
`route-safety.yml` and `pr-checks.yml` trigger on `pull_request` only, neither has ever
run on a push to main — so every new PR would open red. Four commits now fix that.

Nothing is pushed. Nothing has a PASS bound to the final SHA yet.

## Branch `fix/unbreak-main-pr-gates` — final SHA `dd350fbc` (4 commits)

| SHA | What |
| --- | --- |
| `f9014965` | Recognise portal + owner auth gates; keep reset-password visible |
| `6ace6629` | Previous session handoff (docs only) |
| `efc01c6c` | Scope portal gate to portal; ignore commented-out gates; print baseline reasons |
| `dd350fbc` | Close fail-open comment bypass |

### Review history — three rounds, every one found something real

- **Round 1** on `b835d277`: Claude adversary **FAIL**, codex **FAIL**. Both caught that
  exempting `app/api/portal/auth/**` wholesale deleted the only automated signal pointing
  at a live account takeover. Both also caught that the emoji fix used a lucide icon,
  which reds `check:no-lucide`.
- **Round 2** on `6ace6629`: Claude **PASS** with a new P2 (gate tokens were global, so a
  homeowner JWT would vouch for a contractor route); codex **FAIL** on substring gate
  detection. Both closed in `efc01c6c`.
- **Self-probing** then found two defects in my own fix that no reviewer had reported:
  - `865b5cc0` — a string containing ` // ` swallowed a real gate on the same line,
    falsely flagging a gated route. Fails loud, not open.
  - `dd350fbc` — `x;//requireOwner(` was not stripped, so a route whose only gate was
    commented out scored as **gated**. **Fails open.** This is the most serious defect
    found all session and it was mine.

Edge-case matrix for comment stripping is now 10/10 (was 7/10, then 9/10).

### Verification at `dd350fbc`

Run in an **isolated worktree** (`/Users/phill-mac/.claude/worktrees/ra-verify-clean`,
detached at `dd350fbc`, `node_modules` symlinked) because reviewers were live in the main
worktree:

- route-safety **PASS** — 518 routes, 0 new, 5 baselined, weakness marker printing
- `audit:api` **PASS**, `check:no-lucide` **PASS**, `check:no-emoji` **PASS**
- `type-check` **PASS**
- `scripts/__tests__/audit-api-routes.test.ts` **12/12**

### NOT done

**No review verdict is bound to `dd350fbc`.** The round-3 reviews were dispatched against
`efc01c6c` and two commits have superseded it. A confirming round on `dd350fbc` is
required before push. Codex's round-3 run had not returned when the session ended.

## Open risk — founder decision, unchanged

`app/api/portal/auth/reset-password/route.ts` sets a new password knowing **only** the
account email — no emailed verification code — then mints a 30-day portal JWT. One
unauthenticated POST is a full homeowner-portal takeover. The route's own comment records
this as a deliberate product decision, so it was not changed. This work makes it louder
(it now prints `<-- OPEN WEAKNESS, not an accepted exception` on every CI run) but does
not fix it. Needs its own ticket before the portal is exposed to real users.

## Traps worth knowing

- **`handoff-loop.sh` is not a superset of CI.** It omits `check:no-lucide`, `check:ssot`,
  `check:standards`, `check:au-english`, `audit:ai`, `audit:api`. Local green nearly
  shipped a red PR.
- **Reviewer agents plant probe routes in the worktree they review.** Running a gate
  concurrently gives a garbage result — it cost me a false conclusion this session. Use a
  separate worktree, and check `git status` for stray `zz-*` before pushing.
- **The pr-release-gate hook blocks multiline git commands and shell substitution around
  git.** Use `git commit -F <file>`.

## Pick up here

1. Confirm no stray `zz-*` probe routes remain in the main worktree (`git status --short`).
2. Dispatch a confirming review bound to `dd350fbc`.
3. If clean: push, then open the PR with full scope enumerated — opening it *is* merging it.
4. Rebase `fix/pipeline-review-before-pr` (`481b189d`, docs-only, already reviewed) onto
   the new main and ship it.
5. Raise reset-password with the founder.

## Do not redo

- Do not path-exempt `app/api/portal/auth/`. That was the round-1 P0.
- Do not fix the emoji with lucide. Use `RAIcon`.
- Do not re-derive that the three original findings were false positives — confirmed twice.
- Cleanup: `/Users/phill-mac/.claude/worktrees/ra-verify-clean` is a scratch worktree with
  a symlinked `node_modules`; remove it with `git worktree remove` when done.
