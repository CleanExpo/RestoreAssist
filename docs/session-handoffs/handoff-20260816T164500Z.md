---
type: session-handoff
name: handoff-20260816T164500Z
description: Release-gate honesty pass, seven merged PRs, and the meter-photo path traced to source - deployed and BYOK-wired, but never run live
updated: 2026-08-16
---

# Session Handoff - 2026-08-16T16:45Z (2026-08-17 02:45 AEST)

> Phase 0 gate: `bash scripts/handoff-loop.sh` in `D:\RestoreAssist` -
> **`RESULT: green (with skips: tests)`, exit 0, `failed: (none)`**.
> Log: `.handoff-logs/handoff-20260816T163829Z.log`.
> **Seven PRs merged. Zero open** (`gh pr list --state open` returns `[]`).
> `main` at `bade68fd`. Production healthy - probed 2026-08-16T17:10:17Z:
> `{"status":"ok","checks":{"database":{"status":"ok","latencyMs":3},"env":{"status":"ok"}}}`.
> Nothing is running that this handoff depends on.

---

## 1. Summary

**Phase 0.5 state: WIP.** Pi-Dev-Ops work is additionally deferred and listed in
section 7.

**Definition-of-Done: 3 of 5.**

This was drafted as `SHIPPED, 4 of 5`. Two rounds of independent review rejected
that, and both were right. The skill's rule is that any NO forbids SHIPPED, and
naming a deviation does not convert it into a pass - the first rewrite tried to
keep the classification by explaining the exception, which is the same move in
quieter clothes.

The second NO is one I introduced myself and then failed to carry through:
correcting SHIP-DELTA to say the meter-photo path has **never been run live**
falsifies criterion 5, which I had left at YES pointing at the very section I had
just weakened. Correcting a claim in one place and leaving the score that rested
on it untouched is how a document ends up self-contradicting.

Seven PRs did merge, and that is unaffected - the work is real and deployed. It is
the *session* that does not meet its definition of done, not the merges.

1. **Tasks done or deferred with an owner** - YES. Everything unfinished is in
   section 7 with an owner named.
2. **Tests ran green, Phase 0 exit 0** - YES. The gate exited 0 and printed
   `RESULT: green`. Local `tests` were **SKIPPED by design** (the DB suites
   truncate `users`/`workspaces`/`organizations`, so the gate refuses to key them
   off `DATABASE_URL`). CI ran them, and the step list - not the job conclusion -
   proves it: job `95192622417` of workflow `PR Quality Gates`, step 23 `Apply
   migrations for DB-gated tests` => success, step 24 `Unit tests` => success, and
   its log ends `Test Files 843 passed (843)`. **Zero skipped files**, which is the
   number that matters: a suite that "passes" because its DB-gated files skipped
   has bitten this repo before. `Quality Checks` was SUCCESS on all seven merged
   PRs (2012 `ad2a5a22`, 2016 `d041de70`, 2017 `071bc606`, 2018 `14acd4fc`,
   2019 `c7be1a25`, 2020 `58af26aa`, 2021 `bade68fd`). Only CI scores.
3. **`git status` clean and `git stash list` empty** - **NO, and it stays NO.**
   Status is clean; the stash is not. `stash@{0}: On fix/build-sh-fail-closed: S6c
   guard WIP - not for this release` is pre-existing, from another branch and
   another session. The criterion's stated meaning - nothing uncommitted belongs in
   the change - is met, and that is now proven rather than asserted: the stash
   touches exactly `scripts/build.sh`, this change touches exactly
   `docs/session-handoffs/handoff-20260816T164500Z.md`, and the two sets are
   disjoint. The literal text is still unmet. Clearing it belongs to whoever owns
   `fix/build-sh-fail-closed`; it is not this session's stash to drop.
4. **Work is PR'd** - YES. Seven PRs, all merged, SHAs above.
5. **Demonstrable user-visible outcome** - **NO.** This was YES in the draft. The
   corrected SHIP-DELTA below establishes that the moisture-meter path is wired,
   BYOK-correct and deployed, but that **no live run has ever been performed** -
   the only automated coverage mocks the model call. Deployed and demonstrable are
   different claims, and only the first is evidenced. RA-1109 asks for a
   demonstrable outcome, not a green suite.

**Notable: `type-check` passed this gate for the first time.** The same gate
earlier today reported `failed: type-check`, because the script was
`NODE_OPTIONS="..." tsc --noEmit` - a POSIX env prefix `cmd` rejects, so tsc
exited 1 *without ever running*. PR #2012 replaced it with a cross-platform node
runner and merged today, so the column now measures a type result rather than a
shell parse error.

## SHIP-DELTA

**What shipped: the moisture-meter photo path is wired end to end on the customer's
own Anthropic key, and it is deployed. No live run has been performed on it.**

I first wrote this section as "a paying human can photograph a moisture meter and
have it read into a drying log computed against IICRC S500:2021 dry standards."
Three of those clauses do not survive contact with the source. The corrected
version is below, along with what was wrong, because the wrong version is the one
that would have been said out loud at Gold Coast.

PR #2014, `feat(inspection): wire meter-photo capture to the BYOK vision route`,
merged 2026-08-16T05:21:56Z as `3e9d65f9`. Nobody had checked it until now.

**Proven, with the file that proves it:**

- **BYOK is real, with no platform fallback.**
  `app/api/vision/extract-reading/route.ts:133` calls
  `resolveWorkspaceAiKey(userId, "ANTHROPIC")` and returns `402 KEY_MISSING` when
  absent. There is no `process.env.ANTHROPIC_API_KEY` path in the route. Model is
  `claude-sonnet-4-6` via `lib/services/ai/anthropic-gateway.ts:47`.
- **The capture-to-row path exists**, in two requests rather than one. The extract
  route persists **nothing** - its only DB call is a subscription read
  (`route.ts:49`). After the technician confirms, the client posts separately to
  `/api/inspections/[id]/moisture`
  (`components/inspection/MeterPhotoCapture.tsx:265`), which writes the
  `MoistureReading` row plus an audit row (`.../moisture/route.ts:158,163`).
- **Deployed and guarded, with the inference controlled.** Probed
  2026-08-16T17:10:17Z: `/api/vision/extract-reading`, `/api/admin/seed-demo` and
  `/api/portal/generate` each return **401**. Independent review objected that a
  catch-all auth layer could return `401` for a route that is not there, which
  would make the probe meaningless. Tested rather than argued -
  `/api/vision/extract-reading-nonexistent-xyz` returns **404**,
  `/api/admin/seed-demo-nonexistent-xyz` returns **404**, and
  `/api/__definitely_not_a_route__` returns **404**. There is no catch-all guard,
  so on this deployment `401` does discriminate present-and-guarded from absent.
- **Which version is deployed is established from the deploy record, not the
  probe.** GitHub deployment `5933095445`, environment `Production - restoreassist`,
  `sha bade68fd`, state `success`, 2026-08-16T16:38:45Z. #2014's merge `3e9d65f9`
  is an ancestor of `bade68fd` (`git merge-base --is-ancestor`), and
  `git show bade68fd:app/api/vision/extract-reading/route.ts` contains the
  `resolveWorkspaceAiKey(userId, "ANTHROPIC")` call. So the BYOK handler is in the
  deployed tree - that claim no longer rests on the status code.
- **Tests run in CI, inside the `843 passed (843)` run** cited in section 1.

**Wrong in the first draft, corrected:**

- **"Meter-photo OCR" is too broad - only the `moisture` mode reads anything.**
  The `environmental` and `measurement` modes have no extraction endpoint and are
  blocked client-side before any fetch (`MeterPhotoCapture.tsx:723-728`). Section 9
  and the demo runbook already said this; my summary generalised past both. They
  were right and the summary was wrong.
- **"Computed against S500 dry standards" is not what happens.** Nothing in the
  extract path compares a reading to a dry standard; S500 appears there only as
  prose in the vision prompt (`lib/vision/meter-prompts.ts:23`). The real
  comparison happens downstream on read, against a **hard-coded** `IICRC_TARGETS`
  map with a 15% catch-all fallback
  (`app/api/inspections/[id]/monitoring-report/route.ts:21-38`) - and those
  thresholds are hand-authored in **three** separate places
  (`lib/iicrc-dry-standards.ts`, `lib/drying/material-mc-targets.ts`). Table
  lookup against duplicated constants, not computation from the standard.
- **The fixture test does not test the model.** The route test feeds a genuine
  877-byte PNG through the route's guards and even asserts its magic bytes so it
  cannot silently degrade - but `extractMeterReading` itself is **mocked**
  (`app/api/vision/extract-reading/__tests__/route.test.ts:33`). **No test anywhere
  makes a live vision call.** A green suite here proves the plumbing, not that a
  model can read a meter display.

**Therefore not proven, and not to be claimed:** OCR accuracy on a real photo, and
any authenticated end-to-end run on production. A `401` proves a guarded route
exists; a mocked test proves wiring. Neither is a reading.

The demo runbook `docs/demo/meter-photo-and-demo-claim.md` section 6 is the
authority on what may be promised; where it and this section disagree, it wins.

Everything else this session shipped was infrastructure and honesty work. It
made the gate truthful; it did not add customer capability.

## 2. Where it started

A continuous compete loop against `CleanExpo/RestoreAssist`, resumed from
`handoff-20260816T111435Z.md`, whose central claim - "there is no OpenRouter key
on this box, nothing can push" - was false. The key was already at
`%LOCALAPPDATA%\hermes\.env` and the lookup chain was already in memory; the
sweep had simply never been run before the halt was declared.

Constraints: merges founder-only; the release gate blocks and must never be
bypassed; independent review mandatory before any push; blockers go to
`docs/FOUNDER-QUEUE.md` and Linear rather than stopping the loop.

## 3. Decisions locked and what shipped

**Decisions**

- **False greens are withdrawn, not repaired.** A3 demoted to `deferred`, C2 to
  `fail`. The score dropped and became true.
- **Rotation was never required.** The demand was invented, propagated into four
  artefacts, and is withdrawn. Rotations are owner domain; the owner assessed and
  declined. RA-7224 cancelled.
- **The `ageDays < -1` future-date tolerance stays.** A reviewer asked to tighten
  it; declined with reasons. Hours later this session's own evidence, written at
  02:20 AEST, would have failed under the tighter rule. The tolerance earned its
  keep the same day it was defended.
- **No flake fix was built.** Characterisation refuted the premise. Building one
  would have been work against a phantom.
- **Do not build a retrieval stack.** Exa is connected and free; Parallel is the
  metered one. Standing up a crawler, ranker, dedup and a permanent Cloudflare
  arms race to dodge a bill we are not receiving is a recurring tax, not a saving.

**Merged to main (each merge is a production deploy)**

| PR | What |
| --- | --- |
| #2012 | Windows type-check portability - the gate can now measure types |
| #2016 | Release-gate freshness can fail; A3 and C2 demoted |
| #2017 | Prior handoff committed, `FOUNDER-QUEUE.md` created |
| #2018 | Dead `force=true` removed from `.npmrc` |
| #2019 | Session handoff, self-graded WIP-BLOCKED after review rejected SHIPPED |
| #2020 | Stripe rotation requirement withdrawn |
| #2021 | The `.md` allowlist blindness measured rather than argued |

**Committed locally, not pushed** - `feat/anchored-gate` @ `eac27a01` in
`D:\pi-wt-anchored-gate` (Pi-Dev-Ops). Adds `app/server/anchored_gate.py`, a
verifier whose every column is measured from outside the pipeline, with three
states including UNKNOWN. Self-test passes; a sabotaged copy with `all_passed`
forced true trips three assertions and exits 1.

## 4. Key files

| File | Status |
| --- | --- |
| `scripts/release-gate-score.ts` | Modified, merged - freshness, unique-key parsing, strict fences, date round-trip |
| `scripts/__tests__/release-gate-score.test.ts` | Modified, merged - 22 tests, 9 added |
| `docs/evidence/release-gate/1.0.0/C2-secrets-scan.md` | Modified, merged - `fail`, with the allowlist blindness measured |
| `docs/evidence/release-gate/1.0.0/A3-no-sev1-sev2-open.md` | Read-only inspected - already honest, 12 in-scope tickets |
| `docs/FOUNDER-QUEUE.md` | Modified, merged - rotation item struck through in place |
| `.npmrc` | Modified, merged - `force=true` removed |
| `docs/demo/meter-photo-and-demo-claim.md` | Read-only inspected - the demo runbook, shipped in #2014 |
| `app/server/anchored_gate.py` (Pi-Dev-Ops) | Created, committed locally, not pushed |
| `~/.claude/settings.json`, `~/.claude/scripts/or-review.ps1` | Modified outside any repo |

## 5. Running state

Nothing is running.

Production verified healthy after the day's seven deploys:
`{"status":"ok", ... "database":{"status":"ok","latencyMs":5}}`, uptime 1s at the
time of the check - a fresh deploy from the #2021 merge.

Release gate, run `31951792898` (`Release Gate (RA-4956)`, conclusion `success`,
2026-08-16T14:07:34Z): **65/100, zero false points.** That run measured
`c7be1a25`, **not** the current tip - #2020 and #2021 have merged since, both
touching release-gate evidence files, so the score at `bade68fd` is unmeasured.
Re-run before quoting 65 as current. The
previous run scored 75, of which **10 were false** - A3 passing on a Linear query
naming a project that does not exist, and C2 passing on a scan that allowlists
every markdown file. Both withdrawn. All nine machine criteria pass (section B is
20/20). The remaining 35 points are all owner-evidence.

## 6. Verification - exact commands

```bash
# Definition-of-done gate, from the repo root, native Windows git (not WSL)
bash scripts/handoff-loop.sh            # green, exit 0, failed: (none)

# The nine content guards CI runs - run these BEFORE any push
pnpm check:no-emoji ; pnpm check:no-lucide ; pnpm check:spec-docs
pnpm check:encoding ; pnpm check:ssot ; pnpm check:standards
pnpm check:no-verbatim ; pnpm check:marketing-verbatim ; pnpm check:au-english

# Scorer unit tests
pnpm exec vitest run --config config/vitest.config.js scripts/__tests__/release-gate-score.test.ts

# Release gate, the only score that counts
gh workflow run release-gate.yml --ref main -f strict=false

# The C2 allowlist differential (gitleaks 8.30.1, installed this session)
gitleaks detect --no-git --config .gitleaks.toml     # blind to .md
# same scan with the (?i)\.md$ allowlist line removed -> finds it
```

Independent review: `~/.claude/scripts/or-review.ps1 -Path <file> [-Model openai/gpt-5.5] [-MaxTokens 16000]`

## 7. Deferred and open questions

**Deferred**

| Item | Owner | Blocking | Why |
| --- | --- | --- | --- |
| A3 - close or downgrade 12 in-scope Urgent/High | Phill | 5 gate points | Linear decisions only the owner can make; ~10 min on the RA team's active board |
| C2 - narrow allowlist by path, full-history scan, triage | Agent | 5 gate points | Now genuinely doable: gitleaks installed, blindness measured, path-not-class constraint known |
| D1 / D3 - billing walk and reconciliation | Phill | 10 gate points | Needs a real purchase in Stripe test mode |
| E1 / E2 - App Store metadata, TestFlight | Phill | 10 gate points | Apple processing is the only clock that cannot be compressed |
| F1 - monitoring/alerting proven live | Phill | 5 gate points | Dashboards need opening |
| `feat/anchored-gate` - wire into callers | Agent | No | Six files reference `gate_checks`; wiring will turn many rows UNKNOWN, which is correct and will look like a regression |
| `feat/skill-trainer` - 9 local commits, round 1 FAILED | Agent | No | See below |
| Delete or disarm `CleanExpo/chrome-agent` | Phill | No | See risk notes |
| agent-browser stub, Parallel head-to-head | Agent | No | Both started, neither finished |

**Skill-trainer round 1 failed honestly.** The verifier reported **0 of 9 planted
defects caught** - the gate exits 3 before reaching a grader, because the rubric
names criteria the calibration set has never heard of and `cases.yaml` is absent.
The valuable finding: a critic built two stub graders that cannot grade anything,
and **the calibration gate certified both as calibrated**. Its Arm A checked that
a known-bad failed its `must_fail` criteria but discarded verdicts on every other
criterion. The gate was itself rubber-stampable - precisely the defect it exists
to detect. Round 2 should extend `evals/judge.py` (whose `build_prompt` is
already naive) rather than rebuild beside it.

**Open questions**

- **`sprint-0820` does not exist as a Linear label.** Named in a brief; never created.
- **`skill-trainer` had no prior existence** anywhere - not in Pi-Dev-Ops local or
  remote, not in `~/.claude/skills`. Two work packages were written against files
  that did not exist. Confirm whether prior art lives on the Mac Mini.
- **`~/.claude` auto-publishes to the public `CleanExpo/skills-library`** every
  ~15 minutes, so "commit locally, do not push" cannot be honoured for anything
  edited there. Needs resolving before `gauntlet-loop` is amended in place.

## 8. Pick up here

**Do not redo**

- Do not hunt for the OpenRouter key - installed and positive-controlled.
- Do not re-fix the `pr_release_gate.py` hook - fixed and proven live both ways.
- Do not re-characterise the CI flake - RA-7226; the premise does not hold.
- Do not tighten the future-date tolerance - deliberate, test-pinned, and it
  saved this session's own evidence.
- Do not raise the Stripe rotation again - owner-assessed and declined, RA-7224
  cancelled.
- Do not build a demo tenant - `app/api/admin/seed-demo/route.ts` already ships
  one. It creates client, report and inspection inside a single
  `prisma.$transaction` (lines 135-183), is idempotent via a `findFirst` guard
  (line 110), and carries `__tests__/route.test.ts` inside the 843-file CI pass.
  Read from source; **not** run authenticated against production, so treat the
  runbook's output as expected rather than observed.
- Do not build a Parallel replacement - Exa is connected, free, and proven.

**Start here**

1. Ask Phill for the A3 pass in Linear (~10 min, 5 points).
2. Take C2 to green: narrow `(?i)\.md$` to the specific documented placeholder
   files **by path**, run gitleaks over full history, and positive-control it -
   plant a secret-shaped value in a `.md`, confirm the scan finds it, remove it.
   Note Arm B flagged the C2 evidence file itself, which quotes a redacted key
   while documenting it; that file needs an explicit path exemption.
3. Re-dispatch the release gate and report the delta.

**First command to run**

```bash
gh run list --repo CleanExpo/RestoreAssist --workflow=release-gate.yml --limit 3
```

## 9. Risk notes

- **`CleanExpo/chrome-agent` is a public unmodified fork carrying an inherited
  `publish.yml`** - `on: release: [published]`, `environment: pypi`,
  `id-token: write`, `uv publish --trusted-publishing always`. **Actions is
  ENABLED** (`{"enabled": true, "allowed_actions": "all"}`); the empty workflow
  list only means nothing has run yet. Deleting the repo, or deleting the workflow
  **and** disabling Actions, is founder action.
- **`scripts/**` is not type-checked.** `tsconfig.json` excludes it. Proven by
  planting a type error and watching `tsc` still exit 0. Any "type-check green"
  claim about a `scripts/` change is unfounded until RA-7225 lands.
- **A blank review reads exactly like a clean review.** `qwen3.8-max` returned
  zero characters of content while spending 16,000 completion tokens on reasoning.
  `or-review.ps1` now fails loudly, but any other harness calling that model has
  the same fail-open. Check `finish_reason` before believing silence.
- **The demo runbook's section 6 lists what not to promise at Gold Coast** - live
  scope generation is BYOK-correct but **never run or tested**; PDF/Excel export
  rendering unverified; the environmental and measurement OCR modes have no
  extraction endpoint. The seeded claim, its 21 readings and its five S500 clause
  references are fixture data, hand-written rather than RAG-retrieved.
- **A stale comment claims the vision extractor uses the platform key.**
  `lib/services/ai/extract-reading.ts:10-12` still reads "Uses the PLATFORM key
  (process.env.ANTHROPIC_API_KEY)". The code is BYOK - the caller passes a
  workspace-resolved key and there is no env fallback - so the comment is simply
  wrong, and it is wrong in the direction that invites someone to add the fallback
  it describes. Comment-only fix, no behaviour change.
- **IICRC dry-standard thresholds are hand-authored in three places** -
  `app/api/inspections/[id]/monitoring-report/route.ts:21-33`,
  `lib/iicrc-dry-standards.ts` and `lib/drying/material-mc-targets.ts` - with the
  decay constants self-described in-file as "derived from field data". Three copies
  of a safety-relevant table will drift, and the `?? IICRC_TARGETS["other"]` 15%
  fallback means an unrecognised surface type is silently judged against a default
  rather than rejected.
- **A pre-existing stash exists** on `fix/build-sh-fail-closed`, holding
  `scripts/build.sh` only. Not this session's, but it is uncommitted work someone
  meant to keep.
- **`CleanExpo/RestoreAssist` is a PUBLIC repository, and its docs disclose
  internal structure.** Verified this session: `gh repo view` reports
  `visibility: PUBLIC`. Committed docs already contain internal Linear workspace
  URLs (9 files under `docs/`) and the founder's given name (93 files). This is
  **not** a credential exposure - a `linear.app` workspace URL is useless without
  a Linear login, and no secret is involved - so nothing here needs rotating or
  revoking. What it does disclose is org structure, team naming, ticket flow and
  who owns what. That may be entirely intended for an open repo; it is recorded
  because it appears to have accumulated rather than been decided. A gating review
  flagged the URL this handoff was about to add as its 271st instance, so the URL
  was dropped and the instruction reworded - one fewer, at zero cost. Whether the
  other 102 files matter is a founder call, not an agent's, and mass-editing them
  is explicitly not proposed.
- **Six `wt-*` worktrees and ~29 stale remote branches remain untriaged.**
- **Gate timings on this box are unreliable** - the same gate ran in 2 minutes and
  in 10 today. Never quote one timed run as cost.

## 10. Handoff quality check

- Phase 0 verdict read from the gate's own `RESULT:` line, not from a pipe exit
  code. That distinction mattered earlier today, when `tail` returned 0 over a
  `RESULT: BLOCKED`.
- Every merged PR carries a number and a merge SHA; nothing is claimed merged that
  is not.
- **SHIP-DELTA was rewritten after independent review.** The first draft inferred a
  customer capability from three `401` responses. Independent review
  (`openai/gpt-5.5`) called that unsupported, a source read confirmed it, and the
  section now separates what the files prove from what the first draft asserted.
  The overclaim is left visible rather than quietly replaced, because "401 means it
  works" is a mistake that will otherwise be made again.
- The release-gate score names the commit it measured, and says plainly that the
  current tip is unmeasured.
- **The classification was downgraded twice under review, not defended.**
  `SHIPPED 4 of 5` became `WIP 3 of 5`: once because naming a DoD exception is not
  the same as passing it, and once because correcting SHIP-DELTA falsified
  criterion 5, which the draft had left pointing at the section it had just
  weakened.
- Where a reviewer's objection was empirically testable it was tested, not argued.
  The `401` probe now carries its `404` control.
- **Both** Definition-of-Done criteria not met are named - 3 and 5 - with their
  reasoning, and not rounded up. This line said "the one" until the third review
  caught that the downgrade had left it stale: a self-congratulating sentence is
  exactly the kind that survives an edit it should not have.
- Round 1 of the skill-trainer is reported as FAIL with its measured number
  (0 of 9), not as partial progress.

---

Handoff complete. Next safe action: ask Phill to clear A3 in Linear, then take C2
to green - together they are 10 of the 35 remaining gate points and neither needs
a new system built.
