# RestoreAssist Launch-Readiness Review — 2026-07-03

SHIPIT step 3 (launch-review). One merged, de-duplicated report across 7 lenses (build-state, BYOK/AI, connectors, billing, security, PM-journey/UX/growth, ops/deploy) + machine oracle, at origin/main dd907f39 (= live prod). Engineering oracle backing this review: type-check PASS, lint PASS, vitest 3030/0 PASS (see audit-2026-07-03.md). Every finding cites lens + location. Orchestrator independently re-verified ≥1 load-bearing claim per lens (Nexus contract).

## CRITICAL — blocks launch or risks users/money

| # | Lens | Finding | Where | Why | Fix |
|---|---|---|---|---|---|
| C1 | ops+billing | Revenue path broken on prod: STRIPE_PRICE_STANDARD/PREMIUM/ENTERPRISE unset → /billing/upgrade (the page every expired trial is 307'd to, middleware.ts:221-228) throws for every tier | app/api/billing/checkout/route.ts:17-30 (fail-closed, verified); vercel env ls | Zero conversion path for expiring trials; every upgrade attempt 500s | Owner: create 3 Stripe prices + set env vars (~15 min) — then C2 |
| C2 | billing | Two contradictory plan catalogs: /pricing sells Monthly $99/Yearly $1188 (create-checkout-session); paywall /billing/upgrade sells STANDARD/PREMIUM/ENTERPRISE (billing/checkout). Report limits/marketing know nothing of tiers | app/billing/upgrade/TierGrid.tsx:16-31 vs lib/pricing.ts:54-96 | The converting page sells plans the product can't deliver | Pick ONE catalog before launch |
| C3 | billing | PREMIUM entitlement unpurchasable: floor-plan gate reads user.subscriptionTier but NOTHING writes subscriptionTierId (verified: 0 hits repo-wide); webhook stores free-text product name | lib/billing/floor-plan-entitlement.ts:14-23; webhooks/stripe/route.ts:459-471 | $199 buyer gets nothing over $99 — refund/dispute magnet | Map checkout metadata.tier → SubscriptionTier on webhook |
| C4 | billing | Client-controlled priceId: create-checkout-session accepts any priceId from request body; unknown MONTHLY/YEARLY ids trigger dynamic price creation in prod; webhook then grants ACTIVE + 999999 credits regardless of amount | app/api/create-checkout-session/route.ts:60-86,148-200 | Any authed user can subscribe at any price in the Stripe account | Server-side whitelist; delete prices.create fallback |
| C5 | billing | One-time purchases (lifetime + addon packs) fulfilled client-side only: webhook returns early on mode!=="subscription" (verified line 397); PI handler 400s addon/lifetime PIs; delivery depends on buyer's browser hitting verify endpoints | webhooks/stripe/route.ts:397,209-222; verify-subscription/route.ts:95-116 | Pay + close tab = money taken, nothing delivered; webhook retry noise | Fulfill mode:"payment" in checkout.session.completed via metadata.type |
| C6 | billing | Double-billing: no checkout route checks for an existing ACTIVE subscription; second checkout creates a concurrent Stripe sub and overwrites User.subscriptionId (orphaning the first) | billing/checkout/route.ts:33-97 | Real customers charged twice; orphaned subs miss webhook sync | Block when ACTIVE; use subscriptions.update/Billing Portal for plan change |
| C7 | byok | Platform-key leak on the flagship workload: ~18 report-family routes (generate-inspection-report, generate-enhanced, scope-of-works, cost-estimation, interviews, analytics narrative, classify/group-readings/generate-scope, assessments, agents bridge) silently fall back to platform ANTHROPIC_API_KEY via getAnthropicApiKey; POST /api/reports is platform-only via lib/anthropic.ts (both verified). Routes also read only the LEGACY Integration store — customers who add keys via Settings→AI Providers (ProviderConnection) still burn platform spend | lib/ai-provider.ts:144-192; lib/anthropic.ts:5-9; e.g. reports/generate-inspection-report/route.ts:154-164 | Unbounded platform Anthropic spend on the most expensive workload (16k-token reports); contradicts RA-6920/RA-6921 "P0 shipped"; even key-configured customers leak | Migrate family to resolveWorkspaceAiKey; unify on ProviderConnection |
| C8 | byok | audit:ai gate blind spot certifies what it doesn't measure: platformKeyFallback only detects env reads in the route file's own text; lib-delegated reads (getAnthropicApiKey, lib/anthropic, claude-cloud, anthropic-gateway) invisible; pendingMigration=[] implies done; CI green while C7 leaks (oracle: 88 call sites "policy wrapped: no" yet exit 0) | scripts/audit-ai-call-sites.ts:289-297 | The safety gate provides false assurance | Teach gate to flag env-fallback helper imports |
| C9 | ux | Trial promise vs BYOK wall: pricing page advertises "50 inspection report credits" free trial with ZERO mention of the required customer Anthropic/OpenAI key (verified: 0 hits in app/pricing/page.tsx); first disclosure at signup; no platform-key path for core reports (workspace-byok-dispatch.ts:121-126 throws) | live restoreassist.app/pricing; app/signup/page.tsx:214 | Non-technical restorer hits a third-party developer-account wall mid-"free trial" — funnel kill + ACL "free" claim risk | Decision: platform-managed trial key (metered by existing credits) OR BYOK disclosure on every pricing card (copy exists at onboarding/status/route.ts:212) |
| C10 | ops | RESEND_FROM_EMAIL unset in prod — transactional email failing DAILY (proven in runtime logs: /api/cron/trial-reminders fail-closed refusal, 24h window); lib/email.ts:22-25 verified fail-closed | vercel env ls; get_runtime_errors | Trial reminders + all helper email silently dead | Owner: set verified sender (~5 min) |
| C11 | ops | Sentry OFF in prod: no SENTRY_DSN / NEXT_PUBLIC_SENTRY_DSN set; instrumentation inert; gate F1 "alert rules deferred" is actually "no project receiving events" | sentry.*.config.ts:12-13; vercel env ls | Zero error observability for paying customers | Owner: set DSNs + create alert rules |
| C12 | connectors | Plaintext third-party credentials at rest: AscoraIntegration.apiKey and DrNrpgIntegration key + webhook HMAC secret stored unencrypted (bypass credential-vault; Ascora verified at connect/route.ts:89-95) | prisma/schema.prisma:6231,6331-6333 | DB dump = live tenant creds + webhook forgery | Encrypt via credential-vault + backfill (pattern exists) |
| C13 | build-state | Support tickets are a write-only inbox: intake + AI triage + AI draft exist, but NO code path emails a reply to the customer (verified: only "email" in PATCH route is a select field) | app/api/support/tickets/[id]/route.ts:92-106 | Paying customers file tickets and never hear back | Wire draft→sendEmail on reply action |
| C14 | build-state | Prod DB schema drift: 37 tables (FormTemplate, VoiceNote, LidarScan, FloorPlan, LiveTeacherSession…) recorded as migrated, DDL never ran on prod | goals.md #3 / RA-1807; scripts/audit-prod-drift.ts | Features backed by those tables are dead on prod regardless of repo state | Owner-gated prod DDL window (KNOWN, open) |
| C15 | connectors+oracle | /api/v1/connections/status is unauthenticated (audit:api [error], not baselined) AND env-presence-only (no live probes) AND omits 7 real connectors (Drive, Xero, QBO, MYOB, ServiceM8, DR-NRPG, Google OAuth) | app/api/v1/connections/status/route.ts:6-8; lib/connections/status.ts:43-83 | The launch-day readiness manifest would lie in both directions + discloses infra posture to anon callers | Auth (bearer/admin) + optional live probes + add missing connectors; make audit:api errors fail CI |
| C16 | meta | CLAUDE.md on origin/main is UTF-8-corrupt (mojibake through ~60% of the file; `file` reports "data") — verified on the GitHub remote, not just local | CLAUDE.md; gh api contents | Every agent session ingests garbage instructions; rules 1-18 partially unreadable | Restore from a pre-corruption commit + re-apply deltas |

## WARNING — costs users or creates real risk

| # | Lens | Finding | Where |
|---|---|---|---|
| W1 | billing | charge.refunded revokes the whole subscription for ANY refunded charge (e.g. a $20 addon) | webhooks/stripe/route.ts:313-328 |
| W2 | billing | Cancel/reactivate act on email-matched Stripe customer (first hit) not stripeCustomerId; reactivate flips an arbitrary sub; local DB not updated | cancel-subscription/route.ts:70-100; reactivate-subscription/route.ts:28-60 |
| W3 | billing | Tier checkout omits automatic_tax + tax_id_collection (GST!) + CSRF — the other 3 checkout routes have all three | billing/checkout/route.ts:87-95 |
| W4 | billing | Stripe↔DB reconciliation script exists (born from a real prod incident) but is wired to no cron — gate D3 stays open | scripts/reconcile-stripe-subscriptions.ts |
| W5 | billing | /api/subscription/portal lacks the iOS Capacitor guard all 4 checkouts have — the exact Apple 3.1.1 surface already rejected once | subscription/portal/route.ts |
| W6 | byok | Invalid/rotated BYOK key surfaces as opaque 500 (only 429/529 classified); chatbot renders the good 402 as "[object Object]" | lib/services/ai/anthropic-gateway.ts:73-91; components/Chatbot.tsx:362-377 |
| W7 | byok | Cost metering vestigial: budget guard on 2 of ~12 AI surfaces, FAILS OPEN on DB error; logAiUsage at only 4 sites; no per-workspace rate limit | lib/ai/budget-guard.ts:75-106 |
| W8 | byok | Platform-spend routes reachable by any customer: live-teacher/turn, harness/gate-check (session+sub only), pilot/adjuster-session (self-hosted key) | respective routes |
| W9 | security | auth/google-session returns id/email/name/ROLE for any email, unauthenticated — user-enumeration + admin-discovery oracle | app/api/auth/google-session/route.ts:20-42 |
| W10 | security | Durable rate limiter fail-OPEN by default (login/register/reset all fail-open on DB blip; middleware limiter per-instance in-memory) | lib/rate-limiter.ts:183-188 |
| W11 | security | HeyGen/ElevenLabs proxies: bare session, no rate limit, no subscription gate — free-trial users can drive vendor spend | app/api/heygen/route.ts:34; elevenlabs/* |
| W12 | security | CSP retains unsafe-inline + unsafe-eval on script-src (KNOWN F15 residual) | next.config.mjs:104 |
| W13 | connectors | Google Drive reconnect UX broken: dead-letter says "reconnect at Settings→Storage" but that page has no connect button; invalid_grant never clears org token fields (stale "Connected as…"); NO disconnect/revoke path at all | lib/queue/storage-mirror.ts:298-343; settings/storage/page.tsx:75-83 |
| W14 | connectors | Setup activation reads the WRONG token store: cloud_storage check reads NextAuth Account google row, wizard writes Organization.storageProvider* — dead sign-in token red-blocks onboarding despite healthy Drive | lib/setup/checks.ts:259-297 |
| W15 | connectors | Xero/QBO/MYOB stale-token-after-refresh bug (first call after expiry always 401s); Xero/MYOB/Ascora refresh-death → ERROR forever, never DISCONNECTED/reconnect prompt (RA-1308 fix not extended); read/sync paths have no rate limit or timeout; Xero burst config 100 > real 60/min | lib/integrations/*/client.ts |
| W16 | connectors | Ascora webhook verifies then does NOTHING (no idempotency, no processing); ServiceM8 emits job.*/contact.* events the processor drops; DR-NRPG webhook has no replay-freshness check | webhooks/{ascora,servicem8,dr-nrpg}/route.ts |
| W17 | connectors | Resend: no timeout, no durable retry queue; invite/signed-form/winback sends THROW → can 500 the user request after DB write | lib/email.ts:106-1255 |
| W18 | ops | NEXT_PUBLIC_APP_URL unset in prod → team-invite emails link to http://localhost:3000/login (verified fallback) | app/api/team/invites/route.ts:219,312 |
| W19 | ops | OPENAI_API_KEY referenced but unset in prod (RAG embed, admin vectorise fail when invoked) | lib/rag/embed.ts |
| W20 | ux | Self-hosted RestoreAssist-AI tier appears DOWN (ai.restoreassist.app 404s) → "basic" tasks silently fall back to customer BYOK at 5-50x cost | lib/ai/restoreassist-ai-client.ts:34,118-127 |
| W21 | ux | Live hero carries "DRAFT COPY — NEEDS HUMAN VOICE REVIEW before launch" comment (verified line 269); hero promises "Start with your ABN — under 90 seconds" but signup has no ABN field | app/page.tsx:269,337 |
| W22 | ux | Fabricated-looking testimonials component one import away from prod (named people + invented stats); US spelling "Labor" in cost calculator; Margot pages use an entirely different palette; emoji iconography grandfathered on selling pages | components/landing/TestimonialsSection.tsx:9-37; EquipmentCostCalculator.tsx; margot/home/page.tsx:91-97 |
| W23 | build-state | mobile/ Expo app is an orphaned parallel codebase (real tested sync engine, wired to nothing, outside workspace+CI); Live Teacher + similar-jobs endpoints implemented-but-unrouted | mobile/; app/api/live-teacher; similar-jobs |
| W24 | ops | Vercel deploys every main push to prod regardless of CI (branch protection is the only gate); sandbox branch 104/516 diverged | deploy config |
| W25 | build-state | Onboarding blocked features: ABR_API_GUID founder-gated (goals #5); goals #21 iOS sign-in regression memo stale | goals.md |

## SUGGESTION (top items)

S1 connections/status detail leak → strip for anon (folds into C15). S2 zod on only 21/321 mutation routes. S3 test/seed-* lack VERCEL_ENV prod block. S4 SSRF residual: properties/scrape follows redirects post-allowlist. S5 ~10 set-but-unreferenced prod env vars + 60-day-old preview-branch secrets. S6 every-minute storage crons. S7 999999-credit sentinel + product-name string matching for limits. S8 grant-trial-credits.ts grants 30d vs TRIAL_DAYS=15. S9 hero H1/mobile 5s-test; yearly "Best Value" with zero discount; footer Analytics → login wall. S10 INTEGRATION_DEV_MODE has no prod hard-stop. S11 GitHub webhook timingSafeEqual length-check. S12 disconnectIntegration never revokes upstream. S13 dead code (deepseek-api, fetch-with-retry, testimonial/transcribe, WelcomeScreen, VideoDemoSection). S14 simPRO/Dropbox named in tutorial video with zero backend + SMS promised on invite page (advertised-unbuilt).

## Confirmed strong (do not spend launch time here)
Tenancy/IDOR discipline (userId/organizationId scoping, 404-not-403), admin verifyAdminFromDb everywhere, all 21 crons fail-closed timing-safe, no secrets in repo, TLS clean (no NODE_TLS_REJECT_UNAUTHORIZED; audit-env forbids it), iOS ATS cert-pinning, upload magic-byte validation, DOMPurify on the one dangerous sink, 2FA + lockout enforced, BYOK key crypto (AES-256-GCM, masking, no key logging), Stripe webhook signature + replay dedupe, deploy/migration machinery (fail-fast build, drift checks, 15-min prod smoke), PDF/export pipeline, offline web sync, vitest suite 3030 green.

## Verdict (4 lines)
Ready for paying customers: **NO** — AMBER overall; the codebase weight is real and security posture is strong (0 security CRITICALs), but money + trial paths fail.
Fix first: (1) C1+C2+C3 — one plan catalog, prod price env vars, tier entitlement actually provisioned; (2) C9 — trial/BYOK decision (managed trial key or disclosure); (3) C7+C8 — stop platform-key bleed on the report family and fix the gate that hides it.
Then: C5 webhook fulfillment, C10-C11 env (email+Sentry), C12 credential encryption, C13 support replies, C14 prod DDL window.
Security is conditionally green; the dominant launch risk is billing correctness + AI cost leakage, not data breach.
