{
  "buildCommand": "cd packages/backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
  "installCommand": "npm install --workspaces",
  "framework": null,
  "outputDirectory": "packages/frontend/dist"
}
