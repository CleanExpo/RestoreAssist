---
phase: 04-build-test
plan: 01
type: standard
---

<objective>
Verify the current branch (feat/ra-625-626-knowledge-layer) produces a clean production build.
Covers: TypeScript type-check, Next.js production build, lint pass.
Output: Clean build confirming the knowledge layer additions (RA-624, RA-625) are merge-ready.
</objective>

<context>
@.planning/STATE.md
@.claude/PROGRESS.md
</context>

<tasks>

<task type="auto">
  <name>Run type-check</name>
  <action>Run `pnpm type-check` from the repo root. Capture all errors. Fix any errors introduced by recent commits (9989e511 async params fix, 7698baa7 CSP, e5f0a0ab knowledge layer). Do not fix pre-existing errors in packages/videos (Remotion — separate package, ~90 known errors).</action>
  <verify>Exit code 0 from pnpm type-check (app code only)</verify>
</task>

<task type="auto">
  <name>Run lint</name>
  <action>Run `pnpm lint` from the repo root. Fix any lint errors in files touched by recent commits.</action>
  <verify>Exit code 0 or lint output contains no errors (warnings acceptable)</verify>
</task>

<task type="auto">
  <name>Run production build</name>
  <action>Run `pnpm build` (which runs prisma generate + next build). Capture output. Fix any build errors. Note: DB migration is not required for build — prisma generate only.</action>
  <verify>Build completes with "✓ Compiled successfully" or equivalent Next.js success output</verify>
</task>

</tasks>

<verification>
pnpm type-check exits 0
pnpm lint exits 0 (no errors)
pnpm build completes successfully
</verification>

<success_criteria>

- [ ] pnpm type-check passes (0 errors in app code)
- [ ] pnpm lint passes (0 errors)
- [ ] pnpm build completes without errors
- [ ] All fixes committed atomically per task
      </success_criteria>
