node_modules/
.expo/
android/
ios/
*.log

# Expo build output (regenerated by `expo export` / EAS builds)
dist/

# Local env (template lives in .env.example; never commit real values)
.env
.env.local
.env.*.local
