# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
package-lock.json

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# MCP config (contains API keys)
.mcp.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
/lib/generated/prisma

.vercel
storybook-static/
nul

# Playwright E2E testing
/playwright-report/
/test-results/
.playwright/
# Auth state (contains credentials — never commit)
playwright/.auth/user.json

# MCP testing artifacts
.playwright-mcp/
dev-server.log
sketch-tab-test.png
v2-signin.png

.env*.local

# Root-level SQL scripts (development artefacts — migrations live in prisma/migrations/)
/*.sql

# Android signing keystores (NEVER commit)
*.jks
.capacitor-native-notes/keystores/

# Git worktrees (Claude Code isolated workspaces)
.claude/worktrees/

# App Store / Google Play screenshot assets (generated by scripts/generate-store-assets.ts)
store-assets/

.vercel

# Local Claude skills (may contain API keys)
.claude/skills/

# Session artifacts
commit_msg_*.txt
pr_body_*.txt
create_pr_*.bat
verify-settings.png
proxy.ts

# Local tooling
.omx/
.superpowers/

.env*.local

# Multi-PC swarm — local identity per machine (see work-together.md)
.claude/swarm/identity.local.md
# Pi-CEO task-memory (auto-added by Pi-CEO session bootstrap, RA-1374)
.pi-ceo/
